# Changelog

データ内容の変更履歴です。版は `meta.version`（CalVer: `YYYY.MM`）で管理します。構造（スキーマ）の変更は `meta.schemaVersion`（semver）で別軸管理し、ここには構造変更も注記として併記します。

## 2026.08 (2026-08-03 更新)

### 構造変更（2026-08-03・events の日付が主張する範囲を絞る / Issue #69）— `schemaVersion` 3.0.0 → **4.0.0**

**破壊的変更です。** `data/emperors.json` の `events[]` の日付（`date`・`startDate`・`endDate`）が
主張するのは「**年精度 ＋ 在位境界年の月日**」だけになりました。それ以外の月日は年へ丸め、
丸める前の値は **`data/internal/event-date-archive.json`** に全件残してあります（値は消していません）。

- **保存値の深さそのものが主張**になりました（年 `"1211"`・月 `"1211-05"`・日 `"1211-05-07"`）。
  埋め草（`-01-01`／`-12-31`）を廃止したので、**日付文字列の長さが変わります**。
  `datePrecision` は「原典が何を言っているか」の欄なので触っていません（深さは `datePrecision` 以下）
- **`reigns[]` は据え置き**（フル ISO ＋ `datePrecision`）。在位日は `exactDays`・`reignSummary` の
  根なので、主張を絞る対象にしていません。**1ファイルに2つの規約が同居します**
- 移行の実測: 値 **6,258**（event 4,170件）を丸めた。月日を主張する event は **4,337 → 1,173**、
  日精度の値 1,351 → **582**、月精度の値 4,933 → **1,023**
- なぜ絞ったか: 月日を主張していた4,337件のうち原表記 `*Raw` と換算 `source.conversion` を持つものが
  **0件**で、機械では真偽を区別できなかった。検出器を作るたびに母集団が現れ（#34・#55・#56・#62）、
  「主張している以上ゼロにするしかない」ので終わらなかった。**発散していたのは欠陥ではなく主張の面積**
- ゲートを同じ変更で足した（規則 `R-DATE-CLAIM-SCOPE`）: `validate_emperors.py` の
  `check_event_date_format`（深さ ≤ `datePrecision`・**境界年の外に月日を置かない**）と
  `check_event_date_archive`（退避した値の鍵が実在の event を指し、配布物の値がその接頭辞）。
  検出力は `python3 scripts/test_date_claim_scope.py` が合成レコードで測る
- 訂正1件: `jin-simalun.crownPrinceDepositionCount.e001` の `0301-01-10` → **`0301-02-04`**。
  絞り込み `lunar_date_as_iso.py` が**存在しない容器名**（`crownPrinceChangeCount`）を見ていて
  皇太子廃立34件が母集団から外れていたのを直したところ出た。旧暦正月十日を西暦欄へ書いた形で、
  原文（晋書 帝紀第四）の丙寅＝趙王倫の即位（乙丑・`0301-02-03`）の翌日と合う。
  `dateRaw`／`source.conversion` を入れたので `verify_calendar` の **B-5 が再演する最初の1件**になった
- 数え直し: `python3 scripts/screens/date_claim_scope.py`（`--before` で移行前の定義）。
  移行そのものは `scripts/migrations/round_event_dates.py`（一度きり・アーカイブは追記しない）

### 構造変更（2026-08-03・`ages` の深さと `dynastyOrder` の欄 / Issue #69 の続き）— `schemaVersion` **4.0.0** のまま

上の移行と同じ「検査できない主張を配布物から外す」を `ages` と `dynastyOrder` に当てたものです。
**破壊的変更です**（`emperors.csv` の `birthDate`・`deathDate` 列の文字列が短くなり、
`dynastyOrder` 列は未調査の190人ぶんが `null` から空欄になります。列の増減はありません）。

- **`ages.birthDate`／`deathDate` の深さも `birthDatePrecision`／`deathDatePrecision` 以下**に
  揃えました（**42値**を切り詰め。年精度で深さ3が31・月精度で深さ3が11）。
  **アーカイブは作っていません** — 超過分は `-01-01`／`-01` の埋め草40値と、note 自身が
  「参考値」「通説により補完」と書いている2値（`tang-shunzong` の上元2年正月**朔日**換算
  `0761-02-10` → `0761-02`、`qing-dezong` の通説の六月二十八日 `1871-08-14` → `1871-08`）で、
  **原典から読んだ月日は1件もありません**。捨てた値はいずれも note に残っています
- **`reigns[].dynastyOrder` の欄の在り方で3値を分けます**: 数値＝第N代／`null`＝**調べた上で
  歴代に数えない**在位（14在位）／**欄が無い**＝その政権が未調査（53政権・198在位・190人）。
  移行前は `null` が未調査と該当なしを兼ねていて、`meta.catalogs.regimes[].dynastyOrderSurveyed`
  を引かないとレコード単体では区別できませんでした。**未調査ぶんを在位順から機械導出して
  埋めないでください**（導出値は主張ではありません）
- ゲートを同じ変更で足しました: `check_ages`（`ages` の深さ ≤ precision）と
  `check_dynasty_order`（surveyed false ⇒ 欄なし／true ⇒ 欄あり）。
  検出力テストは `python3 scripts/test_date_claim_scope.py` に11件追加
- 副作用1件: `beiwei-tuobayu` の「`deathDate` が最終 `endDate` より前」という既知の矛盾は
  **埋め草の副作用**でした（`0452-10-01` → `0452-10` で月精度の比較が一致し、既知リストから外れた）
- 移行は `scripts/migrations/trim_ages_and_dynasty_order.py`（一度きり）

### 構造変更（2026-08-03・出典と引用を機械で引ける形にする / Issue #69 の続き）— `schemaVersion` **4.0.0** のまま

出典 `source` に `bookId`・`volume` を、引用の器 `quotes[]`（`{bookId, volume?, text}`）を足しました。
**どちらも任意で既存には遡及しません**。`source.page`（散文）はそのまま残るので、
配布 CSV の列は増減しません。

- **`meta.catalogs.books`（89書）を新設**しました。`id` はローカルコーパスの索引の鍵そのもので、
  `scripts/build_books_catalog.py` が**実ファイルから生成**します（書名を人が slug へ振り直すと、
  その対応表を間違えたときに黙って別の書の巻を読みに行く）
- **巻を主張できるのは 32書だけ**です。コーパスから巻の本文を引ける書に限り、
  引けない書（`volumeIndex: null`・`资治通鉴`・`三国志`・`清史稿` など57書）に `volume` を書くと落ちます
- なぜ足したか: `page` は散文なので**巻番号が間違っていても全ゲートが緑で通っていました**（Issue #53）。
  実測では効きます — 元世祖の即位記事の引用は `元史` 巻4 で 8/8 断片が一致し、**巻5・6・17 では 0/8**
- ゲートを同じ変更で足しました。`validate_emperors.py` の `check_quote_containers`（**CI・コーパス不要**。
  形・カタログ参照・巻を引けない書に `volume` を書けない・`source.quote` との同居禁止）と、
  `verify_quotes.py --check-volumes`（**ローカル専用**。巻が引けるか・**引用がその巻の中に在るか**）。
  検出力は `python3 scripts/test_quote_containers.py`（合成24件）と巻の切り出しの標本4件が測ります
- **床（集計に効く判定は構造化引用を1断片以上持つ）はまだ強制していません。** 転記は別段なので、
  入れたのはラチェット（充足数が減ったら落ちる・`source.quote` が増えたら落ちる）だけです。
  不足 4,023容器は `docs/process/RESIDUAL.md` の行にしました
- データの変更は1件（パイロット）: `liu-song-wudi` の `reigns[0].duration` の引用を `quotes[]` へ移し、
  `宋书` 巻3 に在ることを機械で確かめました

### 主張範囲を配布物とサイトから読めるようにした（2026-08-03・Issue #69 の続き）— データ変更なし

上の3つの移行で絞った主張範囲を、**配布物の説明とサイトの表示に反映した段**です。
`data/emperors.json` の値は動いていません（`emperors.csv` も46列365行のまま）。

- **`/about` に引用の主張範囲を出しました。** 引用の全件照合（Issue #38）は完走しないと
  決めたので、**取り下げを無言でやらない**ために内訳を出します（実測: 6,948件中
  **6,657件が底本と機械で照合済み**・人が個別に確認 114件・手元の底本の外 65件・
  **底本で該当箇所を確認できていない 112件**）。112件は誤りと確定したものではないので
  **値は削除しておらず、確認できたことにもしていません**。件数は直書きせず
  `data/quote-refs.json` から引きます（数がずれると説明そのものが嘘になるため）
- **`emperors.schema.json` の欠けを2箇所埋めました**（説明文のみ・構造は不変）。
  単一日付の容器（改元・大赦・立后・皇太子廃立）の `date` に**深さ＝主張**の規約が
  書かれていなかったのと、`datePrecision`（3箇所）に**「原典が何を言っているか」の記録で
  あって主張する粒度ではない**ことが書かれていませんでした
- サイト側: 出来事の日付を**保存値の深さのまま表示**するようにし（`datePrecision` による
  丸めを廃止。移行後の実データで丸めが働く値は0件で、残すと**あとから確定した日付を
  黙って捨てる**）、年表の行を開いて首謀者・結果・note全文・出典を出す機能を**廃止**しました
  （2026-08-03 ユーザー決定。note は作業ログで、その中の引用は「原文を読んだ形跡」）

### 訂正（2026-08-03・西遼徳宗の即位が主張する深さ / Issue #61）— `schemaVersion` **4.0.0** のまま

Issue #55 が「年そのものの決着は別途」として残した1件。`xiliao-dezong`（耶律大石）の**皇帝即位年は
1132年のまま据え置き、月日の主張を取り下げて年精度へ落とした**。

- **年**: 遼史巻三十の西遼附録は即位を「以甲辰岁二月五日即位」としか書かず、甲辰歳は**1124年**。
  しかし金史巻百二十一は同じ年について「天会二年，辽详稳挞不野来降，言大石称王于北方」と大石を
  **王**として報じ、天会八年（1130年）にもなお北方で追討の対象にしている。**1124年の大石が
  中央アジア西部の起児漫で帝位に即いてはいない。** 1132年は附録自身の紀年（延慶三年＝康国元年・
  康国十年殁・在位二十年）からの逆算で、**直接書かれた年ではない**
- **月日**: 「甲辰岁二月五日即位，年三十八」は一続きの句で、干支だけを捨てて月日を別の年へ移す
  根拠が原典に無い。よって**1132年の即位・改元・立后には原典上の月日が無い**。
  `reigns[0].startDate` を `1132-02-05` → `1132-01-01`（`datePrecision.start` を `day` → `year`）、
  `eraChangeCount.events[0].date` と `empressInstallationCount.events[0].date` を
  `1132-02-23` → `1132`（いずれも `day` → `year`）へ改めた。**#55 が入れた `1132-02-23` は
  取り下げ**（旧暦二月五日の換算としては正しいが、その月日が1132年に係る根拠が無い）
- **`ages.accessionAge` を 38 → `null`**。「年三十八」も甲辰歳の句に係る。読みどおりなら生年1087年・
  1132年の即位時は数え46歳になるが、**どの史料も書いていない3段の導出**なので構造フィールドには置かない
- **`reigns[0].conflicts` を新設**して、採用値 `1132-01-01` と対立値 `1124-02-21`（甲辰をそのまま採る読み）・
  `1132-02-23`（干支だけを誤りとする読み）を原文つきで残した
- 波及: `reigns[0].duration` と `reignSummary.totalReignDuration` の `approxDays` を 3985 → **4018**
  （1132-01-01 起点で数え直し・`displayYears` 10.92 → 11.01）
- **`meta` と後続3代（`xiliao-renzong`・`xiliao-tianxi`）は触っていない**。崩御1143年は動いていないので
  連鎖は変わらないが、連鎖の先は読んでいない（残量表の行）

### 訂正（2026-08-03・events の日精度 ISO 日付79件 / Issue #55）— `schemaVersion` 3.0.0 のまま

Issue #50 の横展開。絞り込み `scripts/screens/lunar_date_as_iso.py` が挙げた**要読解100件・53人**を
正史ごとに13ブロックへ割って原典に当て直し、**74件を訂正**した（維持25件）。検証段の指摘で
さらに**5件**（兄弟フィールドの取り残し3件・訂正の向きの誤り2件）を直し、計79件。`count` は動いていない。

- **旧暦の月日をそのまま西暦欄へ書いていた型が最多**。13ブロックすべてから独立に出た。
  唐代宗6件（宝応二年七月十一日→`0763-07-11`／永泰元年正月朔→`0765-01-01`／永泰二年十一月十二日→`0766-11-12`）・
  南宋5件・黄巣3件・朱泚1件・遼西夏4件・晋書2件・南朝2件・武則天2件（載初元年九月九日→`0690-09-09`）・
  李自成1件（崇禎十七年四月二十九日→`1644-04-29`）ほか
- **同じ段の別の記事の日を採っていた型**。禅譲では**前朝側の本紀の日**を採る形が繰り返し出る
  （魏文帝の `0220-11-25` は後漢書 孝献帝紀の遜位日 乙卯、梁武帝の `0502-05-01` は南斉書 和帝紀の丁卯）。
  齊明帝の `0494-12-22` は同じ本紀で皇子寶義を晋安王に立てた日
- **前帝の崩御日を写していた型**が2件（西魏文帝＝孝武帝の崩日、東晋穆帝＝康帝の崩日）
- **太陽暦の年またぎが反映されていなかった型**が4件（北魏道武帝2件 `0398-12-02`→`0399-01-24`、
  後唐荘宗 `0923-12-01`→`0924-01-09`、黄巣3件 `0880-12-13`→`0881-01-16`）
- **月精度への格下げ1件**。後漢順帝の永建改元は後漢書に改元記事そのものが無く、日を与えているのは
  正月甲寅の大赦詔だけなので `datePrecision` を `month` にした
- **兄弟フィールドの取り残し3件**（検証段が発見）。即位・改元・大赦が原文の同じ一文にあるのに
  改元側だけを直していた: `wei-wendi.amnestyCount[0]` `0220-11-25`→**`0220-12-10`**、
  `wei-caomao.amnestyCount[0]` `0254-11-01`→**`0254-11-02`**、
  `beiwei-houfeidi-yuanlang.amnestyCount[0]` `0531-10-30`→**`0531-10-31`**。3件とも
  `reigns[0].startDate` は既に正しい値になっていた
- **訂正の向きを直した2件**。梁豫章王（蕭棟）はいったん大寶二年八月戊午（`0551-10-02`）を採ったが、
  梁書・南史の戊午条は廃位から諸王誅殺までを一括して収める条で、大赦の語は南史卷八の戊午条に無い。
  資治通鑑が即位・大赦・改元を壬戌に置き、在位開始日の根拠もこの壬戌なので **`0551-10-06`** に揃えた
- **維持したうち判断を明示したもの**: 清宣統帝の武昌起義 `1911-10-10` は清史稿が奏報到着順で書くため
  原文だけでは起義当日を確定できず維持（note の「八月乙卯＝10月10日」は換算が誤りで乙卯は10月12日）。
  満洲国の康徳改元 `1934-03-01` は太陽暦の時代で干支不一致の対象外。南宋理宗の改元4件は暦計算が正しく、
  「詔の日か施行日か」の規約問題として Issue #66 へ割った
- **`data/screenings.json` に「母集団1307 → 要読解26」を記録**（着手時は1308→100・数字は訂正後）。
  `no-witness` 1281件は種つき標本29件を原典で読み、**反例2件＝取りこぼし率7%（95%区間 1〜23%）**。
  2件とも日干支は合うが旧暦の月・年の同定を欠いた**60日周期の別解**で、現在の検出器は原理的にこの型を
  拾えない（Issue #57）
- **`data/verification.json`** に検証段の体数を記録。53人ぶんを tier どおり（dependent 3体）に立てると
  100体を超えるが、この訂正は `dates` の1軸しか触らないため**正史ごとに束ねた13パッケージへ1体ずつ**に
  減らした。指摘10件のうち値の変更に至ったのは5件
- `data/schema/INCLUSION_CRITERIA.md` の換算暦の記述を実装に合わせた（「グレゴリオ暦」→
  1582-10-15 より前はユリウス暦）。`docs/process/CORPUS_NOTES.md` に daizhigev20 の南史 卷八の
  文字化け区間を追記。`scripts/check_verification.py --rate` がパッケージ単位の体数で落ちるのを直した
- **派生 Issue**: #57（60日周期の別解と検出器の新設）・#58（唐太宗の即位日と即位大赦の1日ずれ）・
  #59（後漢順帝の大赦が本紀7件に対し events 6件）・#60（`source.page` の取り違え5件）・
  #61（西遼徳宗の即位年 1124 か 1132 か）・#66（改元 event の date の規約）・#67（波及漏れ）


### 訂正（2026-08-03・被反乱 event の没日5件 / Issue #50）— `schemaVersion` 3.0.0 のまま

`validate_emperors.py` の `check_death_event_date`（Issue #42 で追加）が警告した7件のうち、
未トリアージだった5件を原典に当て直した。**5件とも event 側の欠陥**で、「在位終了 ≠ 没日」で
正当に食い違うものは1件も無かった（許可リストに残るのは廃位・禅譲後に殺された2件だけになった）。
`count` は動いていない。

- **旧暦の月日を西暦欄へ直書きしていた2件**
  - `liu-song-houfeidi.rebellionSufferedCount.events[2]`: `0477-07-07` → **`0477-08-01`**。
    『宋書』本紀第九 後廃帝紀の「七月戊子夜，帝殒于仁寿殿」の戊子は七月七日で、換算すると
    477年8月1日（`0477-07-07` の干支は癸亥＝旧暦六月十一日で、原文に対応する記事が無い）
  - `qi-yulinwang.rebellionSufferedCount.events[0]`: `0494-07-22` → **`0494-09-05`**。
    『南斉書』本紀第四 鬱林王の「二十二日壬辰」の壬辰は隆昌元年七月二十日＝494年9月5日
    （日数表記と干支が原文内で一致しない件は在位日付側の `conversion` に記録済み）
- **在位終了日を訂正したときに event へ横展開されなかった3件**
  - `liang-xiaoyuanming.rebellionSufferedCount.events[0]`: `0555-10-29` → **`0555-10-27`**。
    廃位は『梁書』本紀第六 敬帝紀の「九月甲辰，司空陳霸先舉義，襲殺王僧辯，黜蕭淵明」＝10月27日で、
    旧値の10月29日は2日後の丙午＝**敬帝の即位日**だった。`outcome` の「丙午（0555-11-01）」も
    **`0555-10-29`** へ訂正（`liang-jingdi.reigns[0].startDate` と一致）
  - `tangmo-shisiming.rebellionSufferedCount.events[0]`: `0761-04-18` → **`0761-04-22`**。
    旧値は『資治通鑑』の甲午（史朝義軍が礓子嶺に至った戦闘記事）で、被弑日は『旧唐書』本紀第十
    肅宗紀の「戊戌，史思明为其子朝义所杀」＝4月22日。在位終了日は既に訂正済みだった。
    同じ旧値を持っていた `personalCampaignCount.events[1].endDate` も訂正し、
    `datePrecision` を `"year"` → `{"start": "year", "end": "day"}` にした（終期だけ日精度）
  - `shiguo-beihan-liujien.rebellionSufferedCount.events[0]`: `0968-11-01` → **`0968-10-23`**。
    『十国春秋』巻105 少主本紀の己酉は天会十二年九月廿九日＝968年10月23日で、
    旧値 `0968-11-01` の干支は戊午。同書の「帝立裁六十余日」とも整合する（8月23日から61日）
- `check_death_event_date` の許可リストに**未消費エントリの警告**を足した。消費されずに残った
  エントリを黙って通すと、同じ (id, 添字) で将来ずれが出ても許可リストが吸ってしまう
- **横展開（未着手・件数のみ）**: 同型を機械で数える絞り込みを `scripts/screens/lunar_date_as_iso.py`
  として残した。日精度の event 日付1307件のうち、**干支不一致92件・旧暦の月日を西暦欄へ直書きの疑い14件
  （重複6件）＝要読解100件**。ただし今回の5件に対する検出力は**5件中1件**（多くの event note は
  干支も旧暦日も書いておらず、レコード内に照合相手が無い）。Issue #55 として起票

### 精度向上（2026-08-03・王莽の即位日を日精度へ / Issue #46）— `schemaVersion` 3.0.0 のまま

Issue #44 の横展開で「日次の標識を持ちながら day 精度でない端点」として最後に残った1件。
原典に月次の見出しが立っておらず、干支だけで戊辰が何月の日か決められるかが未確認だったため分けていた。
『漢書』王莽伝上の**同じ段の3つの干支**（十一月甲子の上奏・戊辰の即真天子位・十二月朔癸酉を
建国元年正月の朔とする改暦）が朔表と3点で一致し、月次が確定した。

- `wang-mang.reigns[0]`: `startDate` null → **`0009-01-10`**（`datePrecision.start` month → **day**）、
  `startYear` 8 → **9**、`duration` 5408日（月精度の推定）→ **5382日（`exactDays` 確定）**、
  `reignSummary` も連動（`isExact` false → **true**）。初始元年十一月朔＝甲辰（0008-12-17）・
  十一月21日＝甲子（0009-01-06）・次の朔＝癸酉（0009-01-15）が原文の干支と一致し、戊辰は十一月25日。
  `fromLunar(8,11,25)` → 0009-01-10。**甲子の64日後の戊辰は癸酉の朔（始建国元年正月朔）を跨いで
  記事の順序と矛盾するため排除される**（60日周期の別解が無いことの論証を `conversion` に残した）
- `endDateRaw` の「地皇4年9月3日（旧暦、新暦10月3日）」を **「地皇4年10月3日庚戌（旧暦、新暦10月6日）」**へ
  訂正。原典は同伝下の「十月戊申朔」「三日庚戌」で、`fromLunar(23,10,3)` → 0023-10-06（庚戌）と
  干支まで一致する。`endDate` 自体は 0023-10-06 で正しく、raw だけが旧暦月・西暦日ともずれていた
  （新は十二月を正月とする丑正だが、朔表の月立ても同じ丑正で原文と合う）
- `raw`（`"8年 - 23年"`）・`durationRaw`・紹介文の「8年に位に即き」は**旧暦年基準の通説表記として正しい**ので
  変更しない。`ages` の `accessionAge` 53 も**数え年は旧暦年で数える**ため動かない（note の参照だけ更新）。
  `usedEmperorTitleFrom` = `startYear` - 1 の該当は5件 → **6件**
- coverage 実測: 即位日の判別不能 46 → **45** / 374

### 精度向上（2026-08-02・桓玄の即位日を日精度へ / Issue #44）— `schemaVersion` 3.0.0 のまま

Issue #36 F3 で「旧暦年またぎの `startYear` をどちらの流儀で通すか未決」として保留していた1件。
**規約は 2026-07-22 に決着済み**（`reigns[].startYear` は実日付＝太陽暦の年・歴史紀年は
`flags.usedEmperorTitleFrom` が持つ。`data/schema/EMPERORS_SCHEMA.md` の `reigns` 表と
`flags` 表に明記し、`validate_emperors.py` の `check_reigns()`／`check_used_emperor_title_from()`
が両側から強制している）と確認できたため、保留を解いて日精度へ上げた。

- `huan-xuan.reigns[0]`: `startDate` null → **`0404-01-01`**（`datePrecision.start` month → **day**）、
  `startYear` 403 → **404**、`duration` 187日（月精度の推定）→ **170日（`exactDays` 確定）**、
  `reignSummary` も連動（`isExact` false → **true**）。典拠は『晋書』巻十 安帝紀 元興二年条
  「十二月壬辰，玄篡位，以帝为平固王」＝元興二年十二月三日、`fromLunar(403,12,3)` → 404-01-01 で、
  `eraChangeCount`・`amnestyCount` の event 側が既に採っていた日と同じ
- `raw`（`"403年 - 404年"`）と note 各所の「403年12月即位」は**旧暦年基準の通説表記として正しい**ので
  変更しない（同型の `liu-yong-liang` も `raw` を `"25年 - 27年"` のまま `startYear` 26 で通している）。
  `usedEmperorTitleFrom` = `startYear` - 1 の該当は4件 → **5件**
- **横展開**: 日次の標識（干支・日）を持ちながら `datePrecision` が day でない端点を全374在位で数え、
  6件を検分した。うち5件は史料側に即位・崩御の日次が無い（`chenghan-liban`・`beiwei-yuanyu`・
  `tangmo-zhuci`・`tangmo-lixilie`・`houliang-xuandi`）。**桓玄と同型で残るのは `wang-mang` の1件**
  （初始元年十一月戊辰・旧暦年またぎ）で、原典の再読が要るため Issue #46 へ分けた

### 確度の確定（2026-08-02・`confidence` が空文字だった4セル / Issue #42）— `schemaVersion` 3.0.0 のまま

`confidence` が空文字のまま残っていた4セル（2人）を原典に当て直して確定した。**`count` は1件も動いていない**
（`coverage.py` の実測値も変わらない）。`events` は検証段で見つかった横展開漏れの同期1件
（弑逆日 `1360-07-29` → `1360-06-16`）と出典ラベルの訂正2件を直しており、判定内容の変更ではない。

- `yuan-shizu.personalCampaignCount`（3件）→ **high**。「亲征／自将／亲率／亲将／亲讨／车驾亲征」等の
  語彙走査では帝を主語とする用例が計上済みの4箇所（阿里不哥2・乃颜・海都）以外に出ないが、**この走査は
  検出器として不完全**で、同じ本紀に語彙へ掛からない出征記事が2件ある（1253 大理「帝由中道」「帝既入大理」・
  1259 鄂州「帝驻浒黄洲」「庚戌，围鄂」）。この2件を計上しない根拠は語彙ではなく**実権掌握期**の判定で、
  原文が「吾奉命南来，岂可无功遽还」「我奉命南征，岂能中止」と憲宗の命による出征だと明記する。
  車駕の記事63箇所を個別に見ても上都圏を越える系列は至元25年の1つだけで、その北征は皇孫の指揮
  （「诏皇孙抚诸军讨叛王火鲁火孙、合丹秃鲁干」）。計上漏れなしと判断して high
- `yuanmo-xushouhui.personalCampaignCount`（0件）→ **medium**。明史巻123・新元史巻226 しか読んでいなかった
  ので **元史 巻三十八～巻四十七（順帝本紀）を第3の史料として追加で読んだ**。順帝本紀は
  「徐寿辉遣伪将○○陷△△」「徐寿辉兵陷○○」の書式で**本人出征の有無を書き分けない**。境界事例が2件あり
  いずれも計上しない — 至正13年11月の蘄水攻防「寿辉出战，大败，遁入黄梅山」（新元史巻226。本拠地での防戦で
  統軍語を伴わず、明史「寿辉走免」・元史「寿辉遁走」は本人の出戦に触れない。月も三通りに割れる）と、
  至正20年閏5月の太平攻め「遂挟寿辉东下，攻太平」（明史巻123。率いたのではなく挟まれて随行）。
  本人が兵を率いた記述自体は在る（新元史「十九年十二月，寿辉引兵至江州」＝戦場行きではない）
- `yuanmo-xushouhui.rebellionSuppressionCount`（0件）→ **medium**。0件の根拠は「倪文俊の弑逆未遂を誰が
  阻んだか」を明記する史料が無いことで、5史料はいずれも「不果／不克」あるいは「事覚」とのみ書き、
  露見させた主体も鎮圧の主体も書かない。**沈黙からの推論**なので high は出ない
- `yuanmo-xushouhui.rebellionSufferedCount`（2件）→ **medium**。2件の存在は3史料が一致して記録するが、
  **件数側に未決の争点がある** — 倪文俊の弑逆未遂は5史料とも「謀」＋「不果／不克」＋「奔」しか書かず
  兵力行使を記さないため、9節の「実際の兵力行使（挙兵）に至らず、密告・摘発のみで終わった謀反計画は、
  鎮圧・被反乱いずれにも含めない」に当たる可能性がある（→ **Issue #48**）。争点が未決な以上 high は
  成り立たない。events の note が典拠に掲げる「判定基準ルール5・6」が9節に存在しない番号であることも
  同 Issue へ送った
- **付随の訂正（Issue #42 の検証段で発見）**: 2026-07-21 に `yuanmo-xushouhui.reigns[0].endDate` を
  明太祖実録の日精度記事により `1360-07-29` → `1360-06-16`（天定2年閏5月3日）へ訂正した際、
  **横展開されずに旧値が3箇所に取り残されていた**ので同期した — `rebellionSufferedCount.events[1].endDate`
  と同 note の「閏5月16日(1360-07-29)」、`ages.note` と `eraChangeCount.events[2].note` の「閏5月16日」。
  訂正前は event note の「既存deathCause/reignsと一致」という記述自体が偽になっていた
  （構造フィールド同士の突合なので機械ゲートは見ていない）
- 元世祖の乃颜イベントの note が結末の根拠に引く「乃颜以反诛」は**至元二十六年の尚書省臣言の中の言及**で、
  本紀は捕縛・処刑の時期を明記しない（別の年の別記事からの合成）。`outcome` と `endDate` の立て直しは
  **Issue #49** へ分けた（件数3は動かない）
- 付随: `scripts/validate_emperors.py` の許可リスト `KNOWN_EMPTY_CONFIDENCE` を**空にした**。
  以後 `confidence` の空文字は例外なく CI エラーになる
- **出典ラベルの訂正**: `capitalRelocationCount.events[1].note` と `rebellionSufferedCount.events[1].note` の
  「元史 巻四十五 順帝本紀八 **至正十九年十二月条**」は、当該記事が「是岁以后」以下の年末まとめの位置に
  あるため元史が与えるのは年だけで、「十二月」は新元史 巻二百二十六「十九年十二月，寿辉引兵至江州」に由来する。
  ラベルを年までに落とし、月の出所を明記した
- 検証は `adversarial-verifier` 3体（元世祖 facts・徐寿輝 facts・徐寿輝 dates）。指摘9件のうち1件は
  2体が同じ欠陥を挙げた重複で、**ユニーク8件はすべて実欠陥**だった
  （`data/verification.json` の `issue42-confidence` に記録）

### 訂正（2026-08-02・素材 note と原典の食い違い30件 / Issue #36）— `schemaVersion` 3.0.0 のまま

紹介文（Issue #16）の執筆・敵対的検証で上がった後漢初の小政権・五胡十六国（楚／前涼／前趙／成漢）の
指摘30件（本文の A〜F 表28行＋コメント2行）を原文へ当て直して処理した。詳細な台帳は
[docs/qa/issue36-note-corpus-mismatch-2026-08-02/FINDINGS.md](docs/qa/issue36-note-corpus-mismatch-2026-08-02/FINDINGS.md)。

- **構造フィールドを動かしたのは4件**:
  - `chenghan-lishi.crownPrinceDepositionCount` **1 → 0**。李広が「太弟」に立てられたとする記述が
    載記・資治通鑑・華陽国志・十六国春秋のいずれにも無く（4史料とも「勢不許」で終わる）、
    「贬广为临邛侯」は汉王からの降格であって皇太弟の廃立ではない
  - `qianzhao-liucan` 被反乱 `events[0]` の日付 null/`year` → **`0318-09`/`month`**。
    『晋書』元帝紀 太興元年の月見出し「八月」の配下に靳準の弑逆が置かれる
  - `chenghan-lishou.name.templeName` → **中宗**、`chenghan-lixiong.name.templeName` → **太宗**
    （載記末尾「庙曰中宗」「庙曰太宗」）。同じ調査で `chenghan-liqi.name.posthumousName` → **幽公**、
    `chenghan-liban.name.posthumousName` → **哀皇帝** も確定（諡号 98 → 100・廟号 70 → 72）
  - `liu-yong-liang.reigns[0].startDate` `0026-01-07`/`day` → **null/`year`**。
    『後漢書』光武帝紀上は「十一月甲午，幸怀。」と「刘永自称天子。」を別段落に置き、
    干支は光武帝の行幸に付く。年（26）は記事の位置から維持し、`reignSummary` も引き直した
- **続柄・主語の取り違え6件**（張耀霊＝張祚の甥／李班＝李雄の兄子で養子ではない／李寿＝李期の従叔父／
  李載の続柄は典拠なし／桓玄の手詔は否定が落ちて逆の意味・書いたのは安帝／
  李越らを誅殺させたのは李期）、**事実の誤り6件**、**出典ラベルの取り違え1件**
  （李勢の親征 note は『晋書』巻九十八 桓温伝の記述を李勢載記の典拠として掲げていた）、
  **note の陳腐化3件**、**史料対立の記録7件**、**精度・注記5件**
- `chenghan-liban.ages.confidence` を high → **medium**（載記の「時年四十七」と華陽国志系から逆算した
  数え38歳に9歳の開きがあり、その対立が note に無かった）
- **付随**: `data/regime-conventions.json` に成漢（晉書 載記第二十一）の書式・所在を追加（記録6件 → 7件）。
  規則 R-REGIME-FIRST に従い、廟号の補充に先立って政権単位の書式を原典から確定した。
  `data/screenings.json` の母集団を `--update` で 562 → 560 へ引き直し

## 2026.07 (2026-07-31 更新)

### 訂正（2026-07-31・同名別政権の `regimeId` を分割 / Issue #27）— `schemaVersion` 3.0.0 のまま

- **隋末群雄で1つの `regimeId` に合併していた別政権2組を分割**（政権カタログ 87件 → 89件）。v3 移行時に regimes を旧 `dynasty` の `(section, name)` 複合キーから機械導出したため、**同じ調査ブロック内で同じ国号を名乗った別勢力が合併**していた。原典（旧唐書 巻五十六）で出自・拠点・元号がいずれも別と確認のうえ分割:
  - `liangshidu-liang`「梁（梁師都）」＝ 梁師都（夏州朔方の人・朔方に拠り突厥と結ぶ・建元永隆）／新設 `xiaoxian-liang`「梁（蕭銑）」618〜621 ＝ 蕭銑（後梁宣帝の曾孫・岳州で擁立され江陵に拠る）
  - `suimo-chu`（label を「楚（隋末）」→「楚（林士弘）」へ）＝ 林士弘（饒州鄱陽の人・虔州に拠る・建元太平）／新設 `zhucan-chu`「楚（朱粲）」618〜621 ＝ 朱粲（亳州城父の人・冠軍で称帝・建元昌達）
- 付随: `sortOrder` は挿入位置以降（旧43〜87）を +2 で振り直し。`validate_emperors.py` の `KNOWN_REIGN_OVERLAP` から、合併ゆえに必要だった許容エントリ `(suimo-liangshidu, suimo-xiaoxian)` を削除（分割で重複が解消したため）
- **在位・死因・即位経路など人物レコードの判定内容は変更していない**（`regimeId` の付け替えのみ）。判定規約は `data/schema/EMPERORS_SCHEMA.md` の「同じ調査ブロック内の同名国号も別政権として立てる」に明文化
- **サイト側は未対応**: `site/src/lib/emperors.ts` の `dynastyKey` が「国号＋調査ブロック」で組まれているため、この分割だけでは表示は分離されない（別途対応）

### 加算（2026-07-30・kinship 側の語彙カタログを投入）— `schemaVersion` 3.0.0 のまま

- **`meta.catalogs.enums` に `kinshipPersonKind`・`kinshipInclusionReason`・`kinshipRelation`・`kinshipSuccessionCategory` を追加**（enum カタログが14種→19種）。v3 の D3 は「全 enum を ID 化し日本語ラベルは `meta.catalogs` に集約する」としていたが、kinship.json 側の4語彙はカタログが未投入で**ラベルがどこにも無く、消費側が ID を表示ラベルへ解決できなかった**。ラベルは v1 の kinship.json の値をそのまま移した 1:1 のリネームで、**判定内容の変更はない**（`kinshipSuccessionCategory` は `accessionCategory` の8値＋`restoration`（復位）で emperors 側と語彙が異なるため独立キーにした）
- `validate_kinship.py` に `check_enum_catalogs` を追加し、スクリプトの ID 語彙がカタログに収まっていることを検査（kinship 専用キーはカタログ側の余剰も誤り、emperors と共有する `relationToPredecessor` は emperors 専用の `none` を許す）。語彙を増やしたときにラベルの投入漏れが CI で止まる
- 旧サイト（`site/`）を復元して v3 対応させた（詳細は [docs/schema/V3_MIGRATION_PLAN.md](docs/schema/V3_MIGRATION_PLAN.md) の 2026-07-30 追記）。配布 CSV（`public/data/emperors.csv`）の列も v3 に合わせ、`regimeId`・`eraId`・`researchSection`・`standing`・`dynastyOrderSurveyed` を追加し、廃止した `selfProclaimed` 列を削除

### 構造変更（2026-07-29・スキーマ v3 / Issue #22）— `schemaVersion` 2.0.0 → 3.0.0（kinship.json は 1.0.0 → 2.0.0）

新サイトをゼロベースで作り直すにあたり、「サイト側が優先順位ロジックを持たずに表示できる」ことを目標にデータ構造を整理した。**データ値の判定内容（在位・死因・即位経路の軸・回数系指標・note・出典）は変更していない**。設計と移行手順は [docs/schema/V3_MIGRATION_PLAN.md](docs/schema/V3_MIGRATION_PLAN.md)。

- **`meta.catalogs` を新設**: `eras`（時代11区分＝秦漢／三国・西晋／東晋・十六国／南北朝／隋唐／五代十国／宋遼金西夏／元／明／清／近代）・`regimes`（政権87件。同名国号を含めて一意な ID・政権の性格・`dynastyOrderSurveyed`）・`enums`（フィールドごとの ID→ラベル14種）。**日本語の表示ラベルはここにしか置かない**
- **`dynasty`（`name`/`category`/`section`）を解体**し、`eraId`・`regimeId`・`researchSection`・`standing` に置き換えた。`dynasty.section` は31の調査ブロック名で時代区分ではなかったため、時代は独立した enum を新設し、旧値は `researchSection` として保持
- **`dynasty.category` の混在を解消**: 旧「反乱・自称政権」45人には明成祖（永楽帝）・清太宗・後唐荘宗・金世宗・南宋端宗などその政権の正規の皇帝が多数含まれ、政権の性格ではなく称帝経緯のフラグになっていた。政権の性格は `regimes[].category`（`orthodox`/`coexisting`/`rebel`）へ、人物の位置づけは新設 `standing`（`regular` 345人／`rival` 20人）へ分離。`rival` の確定は、調査済み政権では既存の `dynastyOrder`（正史が帝紀を立てたかの個別調査結果）を再利用し、未調査政権の6人（楊侗・楊浩・李裕・李承宏・李熅・朱友珪）は原典で帝紀の不在を確認した
- **全 enum を安定 ID 化**: 即位経路（`accessionRoute.categoryId`）・4軸＋2補助・死因・kinship の `kind`/`inclusionReason`/`relation`/`categoryId`/`relationToPredecessor`。日本語ラベルは `meta.catalogs.enums` から引く
- **`accessionRoute.category`（日本語ラベル）を廃止**し、表示用の確定値 `categoryId` に一本化。実データ 0 件だった `受禅（擁立）` と、スキーマ enum の重複記載4件も整理
- **`flags.selfProclaimed` を廃止**: 旧 `dynasty.category` と81件不一致、`axes` とも不整合で、同じ情報が `axes.throneSource`＋`procedure`＋`standing` で表現できるため
- **kinship.json**: `persons[].section` → `researchSection`、`eraId` を追加（section が一意に時代へ落ちない「晋」34人・「清」8人は個別判定）、`edges[].category` → `categoryId`
- **CI**: `validate_emperors.py` に `check_catalogs`・`check_record_catalog_refs` を追加（カタログ内部整合・レコードの参照整合・`eraId` 非正規化コピーの一致・`rebel` 政権×`rival` の排他・孤児カタログ検出）。`validate_kinship.py` は語彙を ID へ移行

データ内容の版管理は 2026-07-21 に開始しました。以下は開始までの主要な変更の遡及記録を含みます。

### 訂正（2026-07-23・events 混在精度44キーの解消）

- **開始と終了で実確認精度が異なる期間イベントの `datePrecision` に、`reigns[]` と同形式のオブジェクト `{"start": ..., "end": ...}` を導入**（ユーザー確定・スキーマ正式化は `data/schema/ADDITIONAL_SCHEMA.md` 1節、構造変更のため注記）。両端が同精度なら従来どおり単一トークン。該当24イベント・24キー（後漢和帝6・安帝2・東晋元帝1・前秦苻健親征1・斉海陵王1・北魏太武帝1・閩王延羲1・遼天祚帝2・明世宗5・清穆宗4）は既存日付を変えず精度宣言のみ側別化
- **precision が日付深さを上回っていた5イベント群（20キー・4人）を原典個別確認のうえ深化**（sxtwl 自己検証: 干支の月内一意・朔一致・閏月確認・在位範囲）: 明憲宗の靖州苗乱初回（成化三年二月丁巳＝1467-03-26、「二月丁酉朔」一致）・再発（成化十二年三月壬戌＝1476-04-12、「二月乙亥朔」一致）、前蜀王建の元膺の乱（資治通鑑巻二六八の干支 丁未〜己酉＝0913-08-11〜08-13）、前秦苻健の苻菁の乱（通鑑巻一〇〇「六月…庚辰」＝0355-07-05、同年閏六月は非該当）、南燕慕容徳の慕容達クーデター（元興二年四月・「夏，四月，癸巳朔」一致→太陽暦多数月 0403-05 の月精度）
- **斉海陵王（qi-hailingwang）の毒殺月 0494-11→0494-12 を訂正**: 建武元年十一月が旧暦月番号のまま ISO 欄に入っていた（sxtwl 換算で旧暦494年十一月＝ユリウス暦494-12-13〜495-01-11、太陽暦多数月は12月。廃位＝十月辛亥＝494-11-23 の「約1ヶ月後」という note 記述とも整合）。被反乱イベント endDate と `ages.deathDate` を同期訂正。混在精度のオブジェクト化で開始（日精度 11-23）より終了（月精度 11）が手前に見える逆転が可視化されたことによる検出
- **明憲宗・靖州苗乱（再発）の note 引用の誤帰属を訂正**: 旧 note の中略引用『三月壬子……李震大破靖州苗。』は討破を壬子の条に誤帰属していた（明史巻十四の原文は「三月壬子，减内府供用物。壬戌，李震大破靖州苗。」で討破は壬戌）。鎮圧・被反乱の両 note を訂正
- 再発防止として `validate_emperors.py` の startDate/endDate 深さ検査を警告からエラーへ格上げし、オブジェクト形式の構造検査（start/end 両キー必須・同値禁止・単一日付 `date` との併用禁止）を追加。サイト側（`site/src/lib/emperors.ts`）は側別精度でのクランプ表示に対応

### 訂正（2026-07-22・events 日付の全面 ISO 正規化）

- **events の日付（date/startDate/endDate）約1,000件を ISO 8601 へ全面正規化**: (1) 3桁年のゼロ埋め378件（51人）、(2) 元号表記250件（18人）の sxtwl 換算（干支一致・朔検証・年注記突合・在位範囲チェックの自己検証つき）、(3) datePrecision より浅い日付79件（9人）の note 記載原文からの深化、(4) 非標準 datePrecision トークン695件（268件＋装飾つき427件）の解消と、`year`/`month`/`day`/`null` への語彙統一（2026-07-22 ユーザー確定。説明的文言・不確実性は note へ移設して保全）。日精度を確定できない行は先例（姚興・石遵）に従い記載月の太陽暦多数月による月精度とし、不一致は note に明記
- **正規化の裏取りで判明した年・月・干支の取り違え約25件を原典再確認のうえ訂正**: 北宋仁宗（宋史仁宗紀の年見出し取り違え3群〔鎮寧州蠻1034→1035・安化州蠻1035-36→1038-39・湖南徭賊1044→1046〕・王則の乱開始干支・顏秀禁中謀変の日付混入・儂智高の乱終息月・祔廟大赦の干支）、遼天祚帝（乾統の起点を1111年とする+10年誤換算3件）、元英宗（参卜郎の乱 至治二年→三年）、元世祖（建元中統の詔は五月丙戌＝五月十九日）、唐高宗（永淳改元は二月癸未・弘道改元は新唐書の丁巳を採用）、唐憲宗（元和十三年元日の己酉朔は乙酉朔の誤記）、唐宣宗（大中改元は戊申）、唐睿宗（載初元年正月＝周正建子月＝689年12月）、南漢後主（大宝二年＝959年）、後晋少帝（生年月日0914-07-22）、北宋真宗（生年0968-12-23・澶淵親征開始1005-01-03）、西夏恵宗（崩御日1086-08-21＝在位終了日と一致）ほか
- 再発防止として `validate_emperors.py` に events 日付の ISO 形式チェックと datePrecision 語彙の完全一致チェック（year/month/day/null のみ許可）をエラーとして新設。整形により在位 ISO 年範囲チェックが新たに届くようになった称帝前イベント10件を `KNOWN_PREACCESSION_EVENTS` へ正当登録

### 訂正（2026-07-22）

- **exactDays 系統誤差19件を実経過日数へ訂正**（note 全件検証・包括的データチェックの検出分）: 在位日数がユリウス暦ラベルへのグレゴリオ式算術で計算されており、ユリウス暦の閏世紀日（100の倍数年の2月29日）をまたぐ18件が1日不足（han-gaozu・han-wudi・hou-han-hedi・hou-han-xiandi・jin-huidi・qi-donghunhou・beiwei-xuanwudi・sui-wendi・tang-zhaozong・tang-wuzetian・beisong-zhenzong・beisong-huizong・liao-shengzong・liao-daozong・xixia-chongzong・yuan-chengzong・ming-huizong・ming-xiaozong 各+1日）、ming-shenzong が1582年グレゴリオ改暦の10日スキップ無視で10日過大（17562→17552）。`duration.value`/`approxDays`/`reignSummary.totalReignDuration` を連動再計算。再発防止として `scripts/verify_calendar.py`（ユリウス/グレゴリオ JDN による実経過日数ゲート）を CI に追加済み
- **liao-jingzong（遼景宗）即位日 0969-03-13→03-12**: 遼史本紀八は即位を己巳（穆宗遇弑当日）条内に記述しており、conversion 自身が検出していた1日ズレを原文干支換算どおりに解消（exactDays 4962→4963 連動）
- **引用の転記誤字・圧縮改変・帰属誤り22件を訂正**（note 全件検証 6,504 件の検出分）: 一字誤記12件（駆遣→驰遣・惼→惧・聚義/率眗→聚众/率众・燈王/袲之→燕王/袭之・蕭謐→萧谌・弋樂→弋猎・李賛→李赟・上伐燕京の文融合・曲賜→曲赦・为害→所害・絶興→绍兴/大慶→大德・博楽→博罗）、圧縮・改変引用6件（成漢李勢・公孫述・蜀漢劉禅・唐武宗・陳後主・西夏景宗の人名版本統一）、出典帰属2件（隋楊浩「令发诏画敕书而已」は資治通鑑・陳後主「与其子弟日饮一石」は南史/通鑑で陳書に無し）、実在しない引用1件（東晋元帝 deathCause の「敦既得志…」はコーパスに該当文なしのため引用体裁を廃し、実在する晋書帝紀の崩御記事引用に差し替え）。すべて `quote_helper`/grep のコーパス出力から逐語コピーで再取得し、照合台帳 `data/quote-refs.json` の defect エントリ22件を全件消化（現在 defect 0件）
- **換算記録の同期漏れ3件**: liang-xiaoyuanming conversion の fromLunar 引数誤記（(555,9,22)→(555,9,27)）と「南史・梁書とも3日間一致」の誤記述、tangmo-shisiming ages.note の旧日付残存（0761-04-18→04-22 同期）
- **ages.note「null とした」と値の矛盾9件（7人）を note 側訂正**: beisong-shenzong・beisong-zhezong・nansong-duzong・liao-taizong・jin-taizu・jin-shizong・yuan-wuzong。全件、直接記載の生年（＋没年齢等の独立アンカー）からの年号年ラベル差分逆算（ADDITIONAL_SCHEMA 10節）として立証できることを個別確認し、値を維持して note を逆算根拠の明記に訂正（後続パスで値を埋めた際の note 同期漏れだった）。再発防止として `validate_emperors.py` に `check_note_value_sync()` を追加済み
- **ages precision 不整合2件**: sui-wendi birthDate 0541-06→0541-07-21（大統七年六月癸丑の sxtwl 換算・同月内で干支一意）、jin-shizong deathDate 1189-01→1189-01-20（大定二十九年正月癸巳、reigns.endDate と一致）。いずれも precision=day に対して月までしか入っていなかった値を日精度化
- **chen-feidi（陳廃帝）の没年齢矛盾を原典再確認**（task.md 0-3）: 生年554年（梁承聖三年五月庚寅生）と没年齢19歳（「時年十九」＝数え年17とズレ）の矛盾は『陳書』卷四・『南史』卷十・『建康実録』の3書共通で原典由来と確定。生年552年説は逆算のみで直接記載なし（同母弟・伯茂の「時年十八」〔『陳書』卷二十八〕は年齢記載側を支持する傍証）。直接記載2値をそのまま採録し `ages.note` に矛盾を明示・`confidence` を medium へ変更（値の変更なし）
- **データ構造の不統一を解消**（task.md 0-3、判定を伴わない構造是正）: nanming-zongzong 大赦1件の `date` 欠落補完（隆武二年六月→1646-07）・反乱系 events の `name` キー欠落28件に `name: null` 付与・houzhao-shishi の `ages.confidence` 補完（medium）・qin-shi-huang/qin-er-shi の `displayYears` を標準換算（÷365.25・1桁丸め）へ統一（11→10.7・2→3.0、`KNOWN_DISPLAY_YEARS` 許容リスト空に）・han-xuandi 立后3件の `datePrecision` を `day（干支のみ：…、グレゴリオ暦未換算）` 形式へ是正
- **BCE イベント日付の年規約統一 105件**（task.md 0-2）: 前漢の `events[].date`（改元・大赦・立后・皇太子廃立）に歴史年直記（前n年 → `-n`）が混在していたのを、`reigns` と同じ ISO 8601 天文年（前n年 → `-(n-1)`）に統一。レビュー時の見立て（8件）より実際の対象は大きく、BCE イベント全127件中105件を訂正（han-gaozu・han-chengdi の大半は入力時から天文年で正しかった＝混在の実態）。恵帝2件（惠帝四年＝前191年）・後少帝2件（高后六年＝前182年・八年＝前180年、呂后崩御は干支換算で 180BC-08-18 の日精度に確定）・成帝鴻嘉（鴻嘉元年＝前20年、3年ズレ）は原典キャッシュで個別確認のうえ訂正。漢初（太初改暦前）の冬十月〜十二月イベントは実ユリウス年側を採用（規約は `data/schema/ADDITIONAL_SCHEMA.md` に明文化）。成帝陽朔の改元は note で年が判明済みなのに `date: null` だったため `-0023` を付与
- 再発防止として `scripts/validate_emperors.py` に `check_bce_event_years()` を追加（在位 ISO 年範囲チェック＋note「前n年」明記との突合。訂正前データで16件検出・訂正後0件を回帰確認済み）
- **`flags.usedEmperorTitleFrom` の旧値残存3件を訂正**（task.md 0-2）: qianzhao-liuyuan 309→308（晋書載記「永嘉二年、僭即皇帝位」）・suimo-liangshidu 618→617（大業十三年称帝）・yuanmo-mingyuzhen 1363→1362（明史列伝「二十二年春僣即皇帝位」）。いずれもフェーズB在位訂正時の同期漏れで、原典キャッシュで個別確認のうえ訂正。あわせて同フィールドの規約を「歴史紀年ベース（旧暦年またぎの十二月称帝等では `reigns[0].startYear` より1小さくなる。該当4件は正当）」と確定し `data/schema/EMPERORS_SCHEMA.md` に明文化、`validate_emperors.py` に `check_used_emperor_title_from()` を追加（訂正前データで3件検出を回帰確認済み）
- **`reignSummary.totalReignDuration` の同期漏れ9件を再計算**（task.md 0-1）: フェーズBの日付訂正時に `reigns[].duration` 側のみ更新され summary 側が旧値のまま残っていた han-wudi・han-zhaodi・han-xuandi・han-yuandi・wei-wendi・wei-caomao・shuhan-zhaoliedi・shuhan-liushan・nansong-ningzong の `approxDays`/`displayYears` を `reigns` の合計から再計算（最大 366 日のズレ）。shuhan-liushan は reigns 側の月精度格下げ済みに対し summary が `isExact: true` のままだった内部矛盾も解消（`isExact: false`・`needsPreciseDays: true` へ）
- 再発防止として `scripts/validate_emperors.py` の `check_reign_summary()` に totalReignDuration 検証（approxDays 合計・isExact/needsPreciseDays・displayYears 年換算）を追加

### 追加・変更（2026-07-21）

- **ライセンス確定**: データ・調査メモ文章を CC BY 4.0、コードを MIT の二重ライセンス構成として宣言（`meta.license` 新設・`data/LICENSE` 配置）
- **`meta.version`（CalVer）新設**、`meta.source` を再定義（primary＝正史原典、Wikipedia「中国帝王一覧」は収録候補リストの初期洗い出し用として位置づけを明確化）
- **在位期間出典の正史差し替え完了**: `reigns[].duration.source` 全365人・約350件を Wikipedia 由来から正史原典（書名・巻名・原文引用・暦換算つき）へ差し替え。過程で旧暦→西暦換算誤り等の日付訂正を約90件実施（詳細は `meta.reignDurationSourceBlocks`）
- **CC BY-SA 混入スクリーニング**: 全 note 約126万字を jawiki 記事本文と突合し、近似一致の3名4箇所（西燕慕容永・更始帝・夏赫連昌）を原典準拠の表現に書き換え
- **`ages` 生没日付の ISO 正規化**（62件のゼロ埋め・旧暦月転記の補正）
- 構造変更: `schemaVersion` 2.0.0 — 未使用の `sources.wikitextLines` をフィールドごと削除、`reigns[].duration.source` に `quote`/`conversion` を正式追加
- 配布 CSV に `wikidataId` 列を追加（40→41列。全365行に Wikidata QID を収録）

### 追加（2026-07-20）

- **唐哀帝（`tang-aidi`）を追加収録**: 収録漏れが判明したため原典調査のうえ追加。収録数 364 → **365人**

### 初版（2026-07-18）

- 全12項目 × 364人の調査完了（`meta.status.overall: "completed"`）
