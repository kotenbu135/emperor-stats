# design-plans — 設計の決定記録と、撮り比べツール

## いま有効なもの

| ファイル | 中身 |
|---|---|
| `SITE_PLAN.md` | **サイトの構成と設計方針。いま有効な正。** 4ページ構成・shadcn/Tremor の使い分け・配色・各ページの方針・決着済みで再提案しないこと |
| `REVIEW_DASHBOARD_2026-07-31.md` | 外部スキル（ui-ux-pro-max）によるレビュー。**ユーザー判断で不採用**（下記） |
| `tools/` | 撮り比べ・計測のスクリプト（Playwright）。回帰確認に使い続ける |

`SITE_PLAN.md` が「これからどう作るか」、[`../AGENTS.md`](../AGENTS.md) が「崩すとビルドが落ちる契約」。
サイトの方針をこの2本以外から引かないこと。

**`REVIEW_DASHBOARD_2026-07-31.md` の指摘は採用しない（2026-07-31・ユーザー決定・再提案しない）。**
同日、**概要ダッシュボードは完成扱い**になった。あの文書は未着手のバックログではなく、
「こういう指摘が出たが採らなかった」という記録として読むこと。

## 削除した記録（2026-07-31）

同日、**旧サイトの設計ドキュメントを全削除した**（ユーザー決定）。サイトを別セッションでゼロから
書き直すため、旧サイトの記述が残っていると新しい実装の判断がそれに引きずられる、という判断による。

- `docs/site-design/` の8本（LAYOUT・PERFORMANCE・IMPLEMENTATION_LOG・REDESIGN_2026-07・TIMELINE・KINSHIP・KINSHIP_LOG・METHODOLOGY）とモックアップ HTML
- `site/DESIGN.md`（旧サイトの設計契約）・`site/README.md`
- このディレクトリの `AUDIT_2026-07-27-visual.md`・`TODO_2026-07-27-visual-seo.md`（2026-07-27 の第2回監査・全16項目とも実装済みだった）
- 2026-07-27 の実装計画11件（`01-*.md`〜`11-*.md`）は同日に削除済み
- スタック選定の経緯（609行）。**いま有効な内容は `SITE_PLAN.md` へ畳んだ**。廃止した18面構成の
  前提が本文に残っていたため、「2本以外から方針を引かない」を成立させるために削除した

**残したもの**は、肖像画アセットの管理（`docs/site-design/PORTRAITS.md` と、ビルド入力である
`docs/site-design/mockups/card-preview/` の webp 150点）だけ。サイトの見た目とは独立して有効なため。

新サイトが今も依存している契約（Server/Client 境界・v3 のラベル解決・`/emperors` のペイロード分離・
単一情報源の一覧・皇帝追加時のチェックリスト）は `site/AGENTS.md` に絞って残してある。

## tools/

Playwright のスクリプト群。`site/` から実行する（playwright はそこにしか解決しない）。
出力先の `rebuild-shots/`・`palette-shots/` などは `.gitignore` 対象（ローカル専用）。
`_` で始まるファイルも `.gitignore` 対象で、使い捨ての計測スクリプトはその名前で置く。
