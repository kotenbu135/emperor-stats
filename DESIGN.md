# DESIGN.md — 中国皇帝統計 デザインシステム

このファイルは **emperorstats.com（中国皇帝統計）** の UI を作るすべてのエージェント・実装者が常時参照する規範です。
値はすべて確定値として扱ってください（「だいたい」で置き換えない）。数値・比率はすべて `data/emperors.json`（`schemaVersion 3.0.0`・365人）の 2026-07-29 時点の実測です。

- 画面ごとの依頼内容とデータ契約: [docs/site-design/CLAUDE_DESIGN_PROMPT.md](docs/site-design/CLAUDE_DESIGN_PROMPT.md)
- 実データのサンプル（14人・全11時代・エッジケース網羅）: [docs/site-design/fixtures/emperors.sample.json](docs/site-design/fixtures/emperors.sample.json)

---

## 1. アイデンティティ

**正式名称**: 中国皇帝統計（英: China Emperor Statistics）
**コンセプト**: 秦の始皇帝から清の宣統帝溥儀まで、実際に「皇帝」を名乗った **365名** の在位・死因・即位経路・宮廷イベントなど全12項目を、**正史原典に1件ずつ当たって** 記録したデータセットと、その多角的な可視化ダッシュボード。

**プロダクトの性格**（デザイン判断の拠りどころ）:

1. **一次資料に基づく学術データセットである** — 見た目の派手さより、出典・確度・母数が読み取れることが上位。すべての数値は原文引用（`quote`）まで辿れる。UI は必ずそこへの導線を持つ。
2. **不完全さを隠さない** — 肖像は 365人中 **150人分しかない**、没年齢は 269人分しかない。欠損は「無かったこと」にせず、正直に、しかし整った見せ方で提示する。
3. **東洋の古典美 × 現代のデータグラフィックス** — 明朝体の威厳と和紙の質感を土台に、データ部分は現代的で読みやすいゴシック体・淡彩のチャートで構成する。骨董趣味に寄せて可読性を落とさない。

---

## 2. デザイン原則

| # | 原則 | 具体的な帰結 |
|---|---|---|
| P1 | **数値には必ず母数と単位が付く** | 「平均寿命 40.9歳」は不可。「平均没年齢 40.9歳（数え年・n=269/365）」と書く |
| P2 | **色だけで意味を伝えない** | 凡例テキスト・パターン・直接ラベルのいずれかを必ず併用する（WCAG 1.4.1） |
| P3 | **和紙の地に浮かせる、沈ませない** | 面は地色（`--paper`）に近い明るさで、境界線と余白で構造を作る。濃い面は要点だけ |
| P4 | **装飾は面積を持たない** | 金（`--gold`）・パーティクル・ガラスは「縁」と「気配」に留める。情報面積を侵食しない |
| P5 | **365件を一度に「描画」しない（DOM からは消さない）** | 一覧カードは全365件を SSR で出したうえで `content-visibility: auto` ＋画像 `loading="lazy"` で描画を遅らせる。チャート・表は仮想化してよい（10章） |
| P6 | **出典がある限り導線を切らない** | 在位（374/374 区間に原文引用あり）・死因・即位経路は必ず根拠（正史の巻名＋原文引用）へ2クリック以内。**イベントは 6,027件中 1,941件（32%）にしか `source` が無い**ので、出典欄は「ある行にだけ出す」設計にする（無い行に空欄・ダミーを置かない） |

---

## 3. カラーパレット

### 3.1 ブランド・基盤色

CSS 変数名は下表の通りに定義してください（他の名前を使わない）。

| 役割 | 変数 | 値 | 用途 | `--paper` に対するコントラスト |
|---|---|---|---|---|
| 帝国赤（プライマリ） | `--seal` | `#8f000d` | ロゴ、アクティブ状態、重要ボタン、印章モチーフ | **9.17:1** ✅ テキスト可 |
| 帝国赤（前景） | `--seal-fg` | `#f9f9f8` | `--seal` 面上の文字 | —（`--paper` と同色。**`--seal` 面上で 9.17:1** ✅）|
| 帝王金（セカンダリ） | `--gold` | `#cca72f` | 枠線ハイライト、王冠アイコン、特別バッジの縁 | **2.18:1 ❌ テキスト・単独の情報表現に使用禁止** |
| 和紙色（ベース背景） | `--paper` | `#f9f9f8` | 全体背景 | — |
| 和紙色（沈み） | `--paper-sunken` | `#f2f1ed` | セクション帯・表のゼブラ・入力欄の地 | — |
| 墨色（テキスト） | `--ink` | `#191c1c` | 主見出し・本文 | 16.28:1 ✅ |
| 栗皮・渋木炭（サブ） | `--ink-muted` | `#5a403e` | キャプション、補足、メタデータ | 8.91:1 ✅ |
| 罫・境界 | `--line` | `#ded9d0` | カード枠、区切り線 |（非テキスト）|
| 罫・強 | `--line-strong` | `#c3bcae` | テーブルヘッダ下、フォーカス外周 | — |
| カード面 | `--card` | `rgba(255,255,255,0.95)` | Bento カード背面 | — |
| フォーカスリング | `--focus` | `#8f000d` | 2px solid + offset 2px | 9.17:1 ✅ |

**ダークモードは実装しない**（`prefers-color-scheme: dark` でも和紙の見た目を維持する）。

**`--gold` の使用規則**: 金はコントラスト 2.18:1 しかない。**文字色・アイコン単色・細い情報罫には使わない**。使ってよいのは (a) カード枠のグラデーション、(b) `--ink` の文字を載せたバッジ地（`--ink` on `--gold` = 7.46:1 ✅）、(c) 装飾ディバイダ、(d) パーティクルの粒子。

### 3.2 時代カラー（11区分・データ可視化の第一軸）

**王朝（政権）は 87 種あり、色を割り当てられない。色は `meta.catalogs.eras` の 11 時代に割り当て、政権は「時代色＋濃淡＋ラベル」で表現する。**

すべて `--paper` に対して非テキストコントラスト **3:1 以上**（WCAG 1.4.11）を満たし、11色の相互ΔE（CIELAB）最小 **20.0**、第2色覚（deuteranopia）シミュレーション後でも最小 **19.6** を確保した検証済みパレットです。**値を目分量で変えないこと**（変えると検証が無効になる）。

| # | eraId | 表示ラベル | 変数 | 値 | 色名 | `--paper` 比 | 面上の文字色 |
|---|---|---|---|---|---|---|---|
| 1 | `qin-han` | 秦・漢 | `--era-qin-han` | `#6a4926` | 銅褐 | 7.69:1 | `--paper`（7.69:1）|
| 2 | `three-kingdoms-jin` | 三国・西晋 | `--era-three-kingdoms-jin` | `#1e5a40` | 緑青 | 7.69:1 | `--paper`（7.69:1）|
| 3 | `eastern-jin-sixteen` | 東晋・十六国 | `--era-eastern-jin-sixteen` | `#7662a4` | 葡萄 | 4.92:1 | `--paper`（4.92:1）|
| 4 | `northern-southern` | 南北朝 | `--era-northern-southern` | `#085868` | 藍鉄 | 7.65:1 | `--paper`（7.65:1）|
| 5 | `sui-tang` | 隋・唐 | `--era-sui-tang` | `#c6784a` | 唐三彩褐 | 3.22:1 | `--ink`（5.05:1）|
| 6 | `five-dynasties` | 五代十国 | `--era-five-dynasties` | `#af7e8f` | 桑染 | 3.21:1 | `--ink`（5.07:1）|
| 7 | `song-liao-jin-xia` | 宋・遼・金・西夏 | `--era-song-liao-jin-xia` | `#3c90dc` | 天青 | 3.21:1 | `--ink`（5.08:1）|
| 8 | `yuan` | 元 | `--era-yuan` | `#214e97` | 青花藍 | 7.66:1 | `--paper`（7.66:1）|
| 9 | `ming` | 明 | `--era-ming` | `#d76a72` | 礬紅 | 3.22:1 | `--ink`（5.06:1）|
| 10 | `qing` | 清 | `--era-qing` | `#bd71b4` | 藕荷 | 3.22:1 | `--ink`（5.05:1）|
| 11 | `modern` | 近代 | `--era-modern` | `#715c05` | 鶯茶 | 6.16:1 | `--paper`（6.16:1）|

- 「色名」列（銅褐・天青・礬紅…）は**読むための説明ラベル**であって伝統色の規格値ではない。**契約は hex 値のほう**。
- **並び順は必ず `meta.catalogs.eras[].sortOrder`**（上表の #）。開始年でソートしない（北魏は十六国期に始まるが南北朝、遼は五代期に始まるが宋遼金夏。時代の年幅は重なる）。
- **ブランド色との衝突回避済み**: 11色すべてが `--seal` `--gold` からΔE 34以上離れている。赤系の時代色（明 `#d76a72`）とブランド赤（`#8f000d`）を隣接させても、色相・明度とも別物として読める。

### 3.3 政権（87件）の表現

政権に固有色は与えない。`meta.catalogs.regimes[].category` の3値で**塗りの質**を変える：

| category | 意味 | 塗り | 輪郭 |
|---|---|---|---|
| `orthodox` | 正統王朝 | 時代色 100% | 時代色 |
| `coexisting` | 並立政権 | `color-mix(in srgb, 時代色 55%, var(--paper))` | 時代色 |
| `rebel` | 反乱・自称政権 | `color-mix(in srgb, 時代色 40%, var(--paper))` ＋ 45°ストライプ（線幅1px・間隔5px・時代色 60%） | 時代色 破線 1px |

政権名は必ずテキストで併記する（`regimes[].label`。`name` は同名国号があり単独では曖昧）。

### 3.4 カテゴリ配色（死因・即位経路）

時代色とは**別のスケール**。相互ΔE最小 28.3、全色 `--paper` 比 3:1 以上。

**死因（`deathCause.category` 8値）**

| id | ラベル | 実数 | 色 |
|---|---|---|---|
| `illness` | 病死 | 161 | `#1d5a7d` 藍鼠 |
| `assassination` | 暗殺 | 96 | `#e26c75` 退紅 |
| `execution` | 処刑 | 35 | `#9c6706` 黄土 |
| `unknown` | 不詳 | 35 | `#8a8a86` 灰（3.29:1）|
| `suicide` | 自尽 | 15 | `#a97cdd` 藤 |
| `disputed` | 諸説あり | 15 | `#8a8a86` 灰 ＋ 45°ストライプ |
| `killed-in-battle` | 戦死 | 7 | `#535922` 鶯 |
| `accident` | 事故死 | 1 | `#1d5f4d` 常磐 |

`unknown` と `disputed` は**同じ灰**で、`disputed` のみストライプで区別する（どちらも「値が確定していない」ことを示す仲間であることを色で表し、性質の違いはパターンで表す）。

**即位経路（`accessionRoute.categoryId` 8値）**

| id | ラベル | 実数 | 色 |
|---|---|---|---|
| `hereditary` | 世襲 | 120 | `#1d5a7d` 藍鼠 |
| `enthroned` | 擁立 | 97 | `#9c6706` 黄土 |
| `self-established` | 自立 | 48 | `#1d5f4d` 常磐 |
| `usurpation` | 簒奪 | 28 | `#e26c75` 退紅 |
| `acclamation` | 推戴 | 23 | `#a97cdd` 藤 |
| `abdication-received` | 受禅（易姓） | 18 | `#284faa` 群青 |
| `succession-unspecified` | 継承（経緯記載なし） | 17 | `#8a8a86` 灰 |
| `inner-abdication` | 内禅 | 14 | `#535922` 鶯 |

**カテゴリ→色の対応は固定**。ページやチャートが変わっても同じカテゴリは同じ色（凡例の色が動かない）。

**凡例・セグメントの並び順も固定する。** 死因・即位経路のような名義尺度は **件数の降順**（上表の順）。`confidence`（高・中・低）・`datePrecision`（日・月・年）のような順序のある enum だけは `meta.catalogs.enums` の並び順に従う。画面ごとに順序を変えない。

---

## 4. タイポグラフィ

```css
--font-serif: "Noto Serif JP", "Noto Serif SC", serif;    /* 見出し・皇帝名 */
--font-sans:  "Noto Sans JP", "Noto Sans SC", sans-serif; /* UI・本文・データ */
--font-quote: "Noto Serif SC", "Noto Serif JP", serif;    /* 漢文引用のみ。SC が先（下記） */
--font-num:   "Noto Sans", "Noto Sans JP", sans-serif;    /* 数字。font-variant-numeric: tabular-nums */
```

ウェイトは Serif 500/600、Sans 400/500/700 のみ読み込む（可変フォントなら 400–700）。日本語フォントは重いので `font-display: swap`、**preload は Serif 600 と Sans 400 の2種だけ**。

**固定サブセット（常用漢字だけを焼き込む等）にしない。** 皇帝名には稀用字（睿・黎・拔）が入り、モノグラムには CJK 拡張B の字が1件出る（明の朱由𨮁）。原文引用には簡体字が入る（374件中292件）。`unicode-range` で分割した動的サブセット配信（Google Fonts の日本語配信相当）を使う。

| トークン | サイズ / 行高 | 書体・ウェイト | 用途 |
|---|---|---|---|
| `display` | 40 / 1.25 | Serif 600 | ページタイトル、ヒーローの皇帝名 |
| `h1` | 32 / 1.3 | Serif 600 | 画面見出し |
| `h2` | 24 / 1.4 | Serif 600 | セクション見出し |
| `h3` | 20 / 1.5 | Serif 500 | カード見出し |
| `kpi` | 44 / 1.1 | Sans 700 + `tabular-nums` | KPI の数値 |
| `kpi-unit` | 16 / 1.1 | Sans 500 | KPI の単位（数値の右に付ける・数値と同じベースライン） |
| `body` | 16 / 1.8 | Sans 400 | 本文・note |
| `body-sm` | 14 / 1.7 | Sans 400 | 表のセル、補足 |
| `caption` | 12 / 1.6 | Sans 400、`--ink-muted` | 母数・単位・出典 |
| `quote` | 16 / 2.0 | Serif 400、`--ink` | 正史の原文引用（漢文） |
| `label` | 12 / 1.4 | Sans 500、`letter-spacing: .08em` | チップ、凡例、軸ラベル |

**規則**

- 日本語本文の1行は **最大 38em**（約38字）。それ以上の幅では段落を伸ばさずカラムを分ける。
- **すべての数値は `tabular-nums`**（ランキングで桁がずれない）。数字は `--font-num`（ラテン優先）で出す。**実装時に、採用したフォントが実際に `tnum` を持つことを必ず目視確認する**（持たない場合 `font-variant-numeric` は黙って無視され、桁ズレに誰も気づかない。無ければ等幅数字を持つフォントへ差し替える）。
- **漢文引用（`quote`）は Serif・原文のまま・字体変換しない**。引用は `「」` で括らず、左に 3px の `--seal` 罫を立てたブロックで示す。引用の直後に必ず出典（`source.page`。例: 「史記 巻六 秦始皇本紀」）を `caption` で置く。
- **引用ブロックだけは `--font-quote`（SC 優先）＋ `lang="zh-Hans"`** を指定する。引用374件のうち **292件（78%）に日本語フォントが持たない簡体字**（为・宫・时・诏・军…）が含まれており、JP 優先のままだと1つの引用の中で JP 字形と SC 字形が混在する。データ側で字体を変換しない以上、字形の統一は表示側の責任。
- 皇帝名は `name.commonName`（最長14字「承天応運啓聖睿文宣武皇帝黄巣」）が入る。**1行固定のレイアウトを組まない**。諱は最長16字（「アユルバルワダ（愛育黎抜力八達）」＝カタカナ混じり）。

---

## 5. スペーシング・角丸・影・モーション

```css
/* spacing: 4の倍数 */
--sp-1: 4px;  --sp-2: 8px;  --sp-3: 12px; --sp-4: 16px;
--sp-5: 24px; --sp-6: 32px; --sp-7: 48px; --sp-8: 64px; --sp-9: 96px;

--radius-sm: 4px; --radius-md: 8px; --radius-lg: 12px; --radius-xl: 20px;

--shadow-1: 0 1px 2px rgba(25,28,28,.06);
--shadow-2: 0 2px 8px rgba(25,28,28,.08);
--shadow-3: 0 8px 24px rgba(25,28,28,.10);

--ease: cubic-bezier(.2,.7,.3,1);
--dur-fast: 120ms; --dur: 160ms; --dur-slow: 240ms;
```

セクション間は `--sp-7`（48px）、カード内パディングは `--sp-5`（24px）、カード間ギャップは `--sp-4`（16px）を既定とする。

---

## 6. マテリアル

### 6.1 Bento Card

```css
.card {
  background:
    linear-gradient(var(--card), var(--card)) padding-box,
    linear-gradient(135deg, rgba(204,167,47,.55), rgba(204,167,47,.06) 45%, rgba(204,167,47,.38)) border-box;
  border: 1px solid transparent;      /* 金糸グラデーション枠線 */
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-2);
  transition: transform var(--dur) var(--ease), box-shadow var(--dur) var(--ease);
}
.card:hover { transform: translateY(-2px); box-shadow: var(--shadow-3); }
```

### 6.2 ガラスモフィズム（`backdrop-blur`）の適用範囲 — **上限あり**

`backdrop-filter: blur(12px)` は **同時に画面上に存在してよいのは 10 要素まで**。内訳は下表で固定し、これ以外の面には使わない:

| 面 | 枚数 |
|---|---|
| ヒーロー / KPI 帯のカード | 最大 **6** |
| 固定ヘッダー・サイドバー・モバイル下部ナビ | **2**（デスクトップ=ヘッダー＋サイドバー / モバイル=ヘッダー＋下部ナビ）|
| モーダル / ダイアログの背景オーバーレイ | **1** |
| | 同時最大 **9** |

**適用してはいけない面**:

- **皇帝カードのグリッド（365枚）** — 不透明な `#ffffff` 地＋`--line` の1px枠＋`--shadow-1` で同じ見た目を作る。ぼかしは使わない。
- 表の行、チップ、凡例、ツールチップ、チャートの内部要素。

> 根拠: 本プロジェクトは過去に、多数のぼかし面と重いスクロール連動で TBT 18秒 / CLS 1.1 の性能事故を起こしている。ガラスは「枚数の限られた面」だけの表現とする。

### 6.3 パーティクル背景

和紙の繊維・金箔片が漂う微粒子アニメーション。**規範**:

- `<canvas>` 1枚のみ。`position: fixed; inset: 0; z-index: 0; pointer-events: none;`
- 粒子数は **1440px 幅で最大 40**、幅に比例して減らす（モバイルは最大 12）。
- 色は `--gold` と `--ink-muted` の 2色、`globalAlpha` は **0.18 以下**。粒径 1–3px。
- 速度は 8px/秒 以下（「漂う」であって「流れる」ではない）。
- **`prefers-reduced-motion: reduce` では静止した1フレームだけ描く**（完全停止・requestAnimationFrame を回さない）。
- タブが非表示（`visibilitychange`）・キャンバスが画面外のときは停止する。
- コンテンツより手前に来ない。テキストのコントラストに影響を与えない（実質的に地色と同化する濃度）。

---

## 7. コンポーネント仕様

### 7.1 KPI タイル（`StatTile`）— **母数キャプション必須**

```
┌──────────────────────────┐
│ 平均在位年数            ⓘ │  ← label（12px・--ink-muted）＋ 補足ポップオーバー
│ 9.86 年                   │  ← kpi（44px・tabular-nums）＋ kpi-unit
│ 365人・approxDays 平均     │  ← caption（12px）: 母数と算出根拠。省略禁止
└──────────────────────────┘
```

**caption には必ず「母数（n=…）」と「単位・暦の前提」を書く。** 実データの母数は以下の通りで、全員分そろっていない項目がある:

| KPI | 値 | caption に必ず書く内容 |
|---|---|---|
| 皇帝総数 | 365人 | 収録基準（生前に皇帝を称した人物のみ） |
| 平均在位年数 | 9.86年 | n=365・`approxDays` 平均 3,597日 |
| 平均没年齢 | 40.9歳 | **n=269/365**・数え年 |
| 平均即位年齢 | 25.5歳 | **n=172/365**・数え年 |
| 最長在位 | 康熙帝 61.9年 | 22,597日・清 |
| 最短在位 | 金 末帝 0日 | 即位当日に戦死 |

`ⓘ` は任意だが、**caption は任意ではない**。

### 7.2 皇帝カード（`EmperorCard`）

一覧グリッドの単位。**リンク（`<a>`）であること**（SEO クロール可能性のため、遅延マウントの内側に置かない）。

```
┌───────────────┐
│  ┌─────────┐  │  肖像 3:4（360×480 の webp）または モノグラム（7.3）
│  │ portrait│  │
│  └─────────┘  │
│ 康熙帝         │  h3 / Serif 500 / 2行まで
│ 聖祖・玄燁      │  body-sm / --ink-muted（廟号・諱）
│ [清] 61.9年     │  時代チップ ＋ 在位年数
└───────────────┘
```

- **カードの左端に幅 4px の縦帯**（カード高さいっぱい・時代色）。位置は固定で、肖像の上に重ねない。
- `standing: "rival"` の人物（20人）は **枠を破線**にし、standing チップを付ける。**チップの文言は `catalogs.enums.emperorStanding[].label`（「対立・僭称の皇帝」）をそのまま出す**。本ファイル中の日本語表記はすべて説明用であり、実装は必ず catalogs を引く（12章-2）。
- 不透明地（`#ffffff`）。ぼかし禁止（6.2）。
- ホバー: `translateY(-2px)` ＋ `--shadow-2`。肖像は拡大しない（史料画像を歪ませない）。

### 7.3 肖像とモノグラム — **215人は肖像が存在しない**

肖像は `data/images/portraits/webp/<emperor.id>.webp`（**360×480 / 3:4 固定**）に **150件のみ**存在する。365人中 **215人（59%）には画像が無い**。時代別の充足率は大きく偏る:

| 時代 | 充足 | | 時代 | 充足 |
|---|---|---|---|---|
| 秦・漢 | 15/35 | | 五代十国 | 10/34 |
| 三国・西晋 | 15/17 | | 宋・遼・金・西夏 | 17/52 |
| 東晋・十六国 | **11/55** | | 元 | 11/18 |
| 南北朝 | **15/69** | | 明 | 18/22 |
| 隋・唐 | 25/49 | | 清 | 12/13 |
| | | | 近代 | 1/1 |

**したがって「肖像がある前提」の画面を作ってはいけない。** 欠落側が正規の状態であり、プレースホルダは一級のコンポーネントとして設計する。

**モノグラム（`PortraitMonogram`）仕様**

- 肖像と**同じ 3:4 の枠**を占める（グリッドが崩れない）。
- 地: `color-mix(in srgb, 時代色 38%, var(--paper))`。細い内枠 1px（時代色 60%）。
  - この濃度なら文字色は全11時代で `--ink` 一択にできる（コントラスト 8.49〜10.97:1。最も暗いのは南北朝 `#085868` の 8.49:1）。これ以上薄いと「読み込み中の空枠」に見え、これ以上濃いと肖像ありのカードと明度が揃わない。
- 中央に **モノグラム字1字**を Serif 600 で大きく置く。字の決め方は次で固定する:
  1. `name.personalName` の末尾が全角括弧で終わる場合は**括弧の中身**を使う（例: `アユルバルワダ（愛育黎抜力八達）` → `愛育黎抜力八達`）。それ以外はそのまま。
  2. その **末尾1字** を採る（`愛新覚羅玄燁` → `燁`、`慕容垂` → `垂`、`愛育黎抜力八達` → `達`）。
- **先頭1字（＝実質「姓」）を使ってはいけない。** 肖像の無い215人の先頭1字は **異なり39種しかなく**、時代順に並ぶ一覧では同じ字のタイルが壁になる（秦・漢は20枚中18枚が「劉」、南北朝は54枚中17枚が「蕭」・14枚が「元」、宋遼金夏は35枚中12枚が「耶」）。末尾1字方式なら **215人で異なり192種**、時代の中ではほぼ全員が別字になる（秦・漢 20/20、南北朝 53/54、東晋・十六国 43/44）。
- `personalName` は365人全員に値があるのでフォールバックは不要。ただし1件だけ CJK 拡張B の字が出る（明の朱由`𨮁`・U+28B81）。**サロゲートペアを1字として扱い**（`[...str].at(-1)`）、フォントに無い場合は諱の全文を小さく出す代替表示を持つ。
- 篆刻風にしたい場合は、角丸 2px の朱枠（`--seal` 30%）を回すところまで。**画像生成・イラスト補完は禁止**（実在しない肖像を作らない）。
- `alt` は `"（肖像なし）康熙帝"` のように「肖像なし」を明示する。装飾扱い（`alt=""`）にしない。

**肖像がある場合の付随表示**

- 出典は `data/images/portraits/jpg/manifest.json` に `artist` / `credit` / `commonsPageUrl` / `license`（PD 148 / CC0 2）がある。個別ページでは肖像の直下に `caption` で「作者 / ライセンス / Wikimedia Commons へのリンク」を必ず出す（帰属義務はないが、本プロジェクトの方針として表示する）。
- 肖像は**トリミングして顔を寄せない**（史料画像として全体を見せる。`object-fit: contain` 相当）。

### 7.4 チップ（`Chip`）

| 種別 | 見た目 | 内容 |
|---|---|---|
| 時代 | 時代色ベタ ＋ 3.2 の指定文字色 | `catalogs.eras[].label` |
| 政権 | 時代色の枠線＋薄塗り（3.3 の category 別） | `catalogs.regimes[].label` |
| standing `rival` | `--seal` の破線枠＋白地 | `enums.emperorStanding` の `label`（「対立・僭称の皇帝」）|
| 確度 `medium` / `low` | 灰の枠＋灰文字 | 「確度: 」＋ `enums.confidence` の `label`（「中」「低」）。`high` は表示しない |
| 女性 | `--gold` 地 ＋ `--ink` | 「女性」（該当は武則天1人） |

高さ 24px、`--radius-sm`、`label` トークン。

### 7.5 ランキング行（`RankRow`）と指標セレクタ

**このサイトのグラフは概要ダッシュボードに集約する。指標ごとにページを増やさない。**
量的な指標（棒が適するもの）はすべて **1つのランキングブロック**の中で切り替える。

ブロックの構成:

```
┌─ 在位年数ランキング ────────────────────────────┐
│ [集計: 皇帝▾] [指標: 在位年数▾] [並び: 長い順⇅]      │  ← セレクタ3つ
│ 365人中 365人（n=365・approxDays 基準）              │  ← 母数キャプション（必須）
│ 1  聖祖（康熙帝）   [清]      61.9年 ████████████    │
│ 2  高宗（乾隆帝）   [清]      60.4年 ███████████     │
│ …（スクロールで全件）                                │
└──────────────────────────────────────────────┘
```

- **集計セレクタ**: `皇帝`（365行）/ `政権`（87行）/ `時代`（11行）。政権・時代を選んだときは値を平均／合計で切り替え、行に「n=◯人」を出す。
- **指標セレクタ**（12指標。すべて1つのブロックの中で切り替える）:

  | 指標 | 出どころ | 母数 |
  |---|---|---|
  | 在位年数 / 在位日数 | `reignSummary.totalReignDuration.displayYears` / `.approxDays` | 365 |
  | 即位時年齢 | `ages.accessionAge` | **172** |
  | 没年齢 | `ages.deathAge` | **269** |
  | 即位回数 | `reignSummary.reignCount` | 365 |
  | 改元回数 | `eraChangeCount.count` | 365（総計681）|
  | 大赦回数 | `amnestyCount.count` | 365（総計1,338）|
  | 立后回数 | `empressInstallationCount.count` | 365（総計278）|
  | 皇太子廃立回数 | `crownPrinceDepositionCount.count` | 365（総計35）|
  | 遷都回数 | `capitalRelocationCount.count` | 365（総計57）|
  | 親征回数 | `personalCampaignCount.count` | 365（総計291）|
  | 反乱鎮圧回数 | `rebellionSuppressionCount.count` | 365（総計1,494）|
  | 被反乱回数 | `rebellionSufferedCount.count` | 365（総計1,853）|

- **並びセレクタ**: 多い順 / 少ない順（在位年数なら「最長 / 最短」とラベルを変える）。
- 指標を切り替えても**ブロックの高さが変わらない**こと（レイアウトが飛ばない）。行の入れ替えは 240ms のフェード＋並べ替えアニメーションまで。
- 母数が 365 未満の指標（即位時年齢・没年齢）に切り替えたときは、キャプションを **「172人に値あり / 193人は調査済み・不明」**のように必ず書き換える。値の無い人を 0 として混ぜない。
- 行: `順位 / 名称 / 時代・政権チップ / 値（tabular-nums・右揃え）/ 値のバー`。
- **1カラム**で全件をスクロール領域に収める（2カラム分割は不可）。バーは行の背景に敷く（`--paper-sunken` の上に時代色 55%）。**値が 0 でも最小幅 2px を描く**（7.7）。
- **静的な HTML の表として出すのは「集計=皇帝・指標=在位年数・並び=長い順」の既定の組み合わせの上位10件だけ**（12指標×3集計×2並び＝72通りを全部静的化しない）。これは遅延マウントの外に置き、クローラが読めること。11位以降と他の組み合わせはクライアント側描画・仮想化でよい。
- 行クリックで **ドリルダウン**（7.10）。皇帝行なら個別ページ、政権・時代行ならその条件でダッシュボード全体を絞り込む。

### 7.6 詳細ダイアログ / 個別ページ

一覧から皇帝を開く動線は **`/emperors/<id>` への遷移**（ダイアログを使う場合も URL を同期し、ブラウザバックで閉じる）。

個別ページの構成（上から）:

1. **ヘッダー**: 肖像 or モノグラム（左）／`commonName`・廟号・諡号・諱・別名・時代チップ・政権チップ・`rival` チップ（右）
2. **人物の概要（`person-summary`）**: SEO のための地の文。**200〜400字の枠だけ用意し、本文は後で書く**（7.12）。ここはデータから機械生成せず、人が書く欄。ダミーテキストで組み、`h2`「人物の概要」を付ける
3. **在位帯**: 複数回即位は8人（**3区間は宣統帝のみ**、他7人は2区間）。帯を分割して描き、`isRestoration: true` の区間には「復位」ラベル
   - 端の精度が `month` / `year` の区間（開始 month 59・year 19 / 終了 month 44・year 13）は、帯の端をぼかす or 破線にする
   - **`startDate` が null の区間が46、`endDate` が null の区間が34ある。** この場合は `startYear` / `endYear` だけで年精度の帯を引く（日付を捏造して端を確定させない）
4. **KPI 行**: 在位日数 / 在位年数 / 即位年齢 / 没年齢（**値が null の項目はタイルごと消さず「調査済み・不明」と表示**）
5. **死因・即位経路**: カテゴリチップ＋ `note` 本文（平均107字・最大433字）＋ 出典
6. **宮廷イベント・軍事の8指標**: 改元 / 大赦 / 立后 / 皇太子廃立 / 遷都 / 親征 / 反乱鎮圧 / 被反乱。各指標は「回数＋展開可能なイベント表」。**1人あたり平均16.5件、最大223件（南宋高宗）** なので、既定は折りたたみ＋最大高さ 480px のスクロール。**出典は `source` を持つイベントにだけ出す**（イベント6,027件のうち `source` があるのは1,941件＝32%。無い行に空の出典欄やダミーを出さない）
7. **出典**: 在位日数の根拠（正史の巻名＋原文引用＋暦換算メモ）。原文引用は 374/374 区間すべてに存在する
8. **外部リンク**: `sources.wikidata` は365人全員に値がある（null 0件）。Wikidata へのリンクを出典ブロックの末尾に置き、構造化データ（JSON-LD）の `sameAs` にも入れる
9. **関連動画**（任意）: `data/emperor-videos.json` に紐付けのある **40人**のみ。無い人には枠を出さない

### 7.7 チャート（共通規範）

- **在位の量的主軸は `reignSummary.totalReignDuration.approxDays`**（データ側が「グラフ用の主軸」と定義している）。表示単位が年のときは `displayYears` を使い、小数第1位まで。
- `isExact: false`（95/365）の値は数値の前に `約` を付けるか、`caption` で「概算を含む」と断る。**混ぜて描いてよいが、断りなく描かない。**
- **ゼロ・極小値**: `approxDays` が 0 の人（金 末帝）や 3日・6日の人が実在する（30日未満が8人）。棒・帯は**最小 2px を必ず描き**、値ラベルを直接添える（0を欠損に見せない）。
- 軸の最大値を自動で「きりのいい数」に丸めない（`nice: false` 相当）。丸めると 22,597日の康熙帝が枠に触れず不自然になる。
- 凡例は必ずテキスト。ツールチップはスクロール枠内では `position: fixed` の自前実装（親要素で切り取られない）。
- ドーナツ/円グラフは**8カテゴリまで**。それ以上は横棒。
- 全チャートに**同じ内容のテキスト表（またはリスト）を SSR で併置**する（スクリーンリーダーとクローラのため。視覚的に隠す場合も DOM には出す）。
- **チャートの構造は `figure` ＋ `figcaption`（見えている要約）＋ 併置テキスト表**の3点で固定する。svg / canvas 本体には `aria-hidden="true"` を付け、**`role="img"` ＋要約 `aria-label` は使わない**（読み上げが表とで二重になり、9章の「`aria-label` に説明を足さない」とも衝突する）。

### 7.8 内訳ブロック（円グラフ）と分類セレクタ

質的な分類（円が適するもの）も**1つのブロックの中で切り替える**。分類ごとにページを作らない。

- **分類セレクタ**（下表の12種。カテゴリ8以下は円、9以上は同じブロックが自動で横棒になる）:

  | 分類 | フィールド | カテゴリ数 |
  |---|---|---|
  | 死因 | `deathCause.category` | 8 |
  | 即位経路 | `accessionRoute.categoryId` | 8 |
  | 帝位の出所 | `accessionRoute.axes.throneSource` | 3 |
  | 帝号の由来 | `accessionRoute.axes.titleOrigin` | 2 |
  | 即位を決めた主体 | `accessionRoute.axes.decidedBy` | 4 |
  | 先帝の最期 | `accessionRoute.axes.predecessorFate` | 5 |
  | 即位の手続 | `accessionRoute.axes.procedure` | 5 |
  | 政権の性格 | `catalogs.regimes[].category` | 3 |
  | 正規 / 対立・僭称 | `standing` | 2 |
  | 死因の確度 | `deathCause.confidence` | 3 |
  | 時代 | `eraId` | **11 → 横棒** |
  | 先帝との続柄 | `accessionRoute.axes.relationToPredecessor` | **23 → 横棒** |

- **9カテゴリ以上の分類（時代 11・先帝との続柄 23）を選んだときは、同じブロックが自動的に横棒に切り替わる**（円で描かない）。切り替わったことがわかるよう、キャプションに「カテゴリが多いため横棒で表示」と出す。**政権（87）は分類セレクタに載せない**（多すぎる。政権の比較はランキングブロックの集計セレクタ＝7.5 で行う）。
- 中央に合計（`365人`／絞り込み中は `N人`）を置く。凡例は必ずテキスト＋実数＋割合。
- **セグメントのクリックでドリルダウン**（7.10）。
- `decidedBy` は配列（1人が複数値を持ちうる）。**合計が母数を超えるので、割合の分母を必ずキャプションに書く**（「延べ N件 / 365人」）。

### 7.9 フィルターバー（ダッシュボード・一覧・探索で共通）

同じフィルタ UI を**概要ダッシュボード・皇帝一覧・皇帝データ探索・王朝データ探索の4画面で共有**する。

条件: フリーワード（`commonName` / `personalName` / `posthumousName` / `templeName` / `aliases` / 政権名）＋ 時代 ＋ 政権 ＋ 死因 ＋ 即位経路 ＋ `standing` ＋ 在位年数レンジ ＋ 没年齢レンジ。

> **かな読みでは検索できない（UI を作らない）。** `data/emperors.json` に読み仮名のフィールドは無い（旧サイトが持っていた読みテーブルは `site/` ごと削除済み）。かな検索を復活させるなら、先に読みテーブルを別途作る必要がある。

- **選択中の条件は必ずチップとして可視化し、個別に×で外せる**。「すべて解除」も置く。
- 常に「365人中 N人」を出す。**ダッシュボードでは KPI・ランキング・内訳のすべてが同じ N に連動する**（一部だけ絞られる状態を作らない）。
- 0件のときは、直前に効いた条件を名指しして外す導線を出す（「『元 × 戦死』に該当なし → 死因の条件を外す」）。
- 入力は 150ms デバウンス。**URL に同期**（共有・戻る操作で復元できる）。画面をまたいでも条件を引き継ぐ。
- モバイルではボトムシートに畳む。適用中の条件数をバッジで出す。

### 7.10 ドリルダウン

ダッシュボードの探索の主動線。**新しいページに飛ばさず、その場でフィルタを積む。**

1. 円グラフのセグメント / 横棒 / ランキング行（政権・時代）をクリック
2. その値がフィルタチップとして積まれ、**画面上のすべてのブロックが再集計される**（KPI の母数キャプションも追随）
3. 積んだ条件は 7.9 のフィルターバーのチップ列に出る。パンくず的に「時代: 唐 › 死因: 暗殺 › 12人」と読めること
4. 絞り込み結果の出口を常に用意する: **「該当する 12人を一覧で見る」**（→ 皇帝一覧へ同じ条件で遷移）と **「表で見る」**（→ 皇帝データ探索へ）
5. 1クリック＝1条件。`Esc` または チップの× で直前の条件だけ外せる（全解除と取り違えない）

### 7.11 データ探索テーブル（`DataTable`）

「皇帝データ探索」「王朝データ探索」で共有する表コンポーネント。カードの一覧が**眺める**画面なのに対し、こちらは**比べる・並べ替える・探す**画面。

- **列の選択**: 既定は6〜7列。列選択メニューで表示列を出し入れできる（選択状態は URL に同期）。
- **並べ替え**: 全数値列でクリックソート（昇順/降順）。ソート中の列はヘッダを `--seal` 下線＋矢印で示す。**null は常に末尾**（0 として扱わない）。
- **絞り込み**: 7.9 のフィルターバーを上部に共有。件数「365件中 N件」を常に表示。
- **行の高さは 44px 固定**、ゼブラは `--paper-sunken`。ヘッダは `position: sticky`。**縦横スクロールは表のコンテナ内**（ページ本体を横スクロールさせない）。
- 数値列は右揃え＋`tabular-nums`、欠損は `--ink-muted` の「不明」（空欄・ハイフンだけにしない）。
- 365行・87行とも**仮想化**する（カード一覧と違い、多列の表は全件 DOM に出すと重い）。ただし SEO 用に、**既定ソート（時代順）での先頭50行**は静的 HTML で出す。
- 行クリックで皇帝の個別ページへ／政権行クリックでその政権に絞り込んだダッシュボードへ。
- 1行目のセルは `<th scope="row">`。表全体に `<caption>`（視覚的に隠してよい）。

**皇帝データ探索（365行）の列**

| 列 | 出どころ | 既定 |
|---|---|---|
| 皇帝名（`commonName`）＋ 諱 | `name` | ✅ |
| 時代 / 政権 | `eraId` / `regimeId` → catalogs | ✅ |
| 在位期間（西暦） | `reignSummary.firstStartYear`〜`lastEndYear` | ✅ |
| 在位年数（日数） | `totalReignDuration.displayYears` / `.approxDays` | ✅ |
| 死因 | `deathCause.category` | ✅ |
| 即位経路 | `accessionRoute.categoryId` | ✅ |
| 即位時年齢 / 没年齢 | `ages.*`（数え年・n=172 / 269）| ✅（没年齢のみ）|
| 改元・大赦・立后・廃立・遷都・親征・鎮圧・被反乱 の各回数 | 8つの `*.count` | 任意 |
| 正規・対立 | `standing` → `enums.emperorStanding` の `label` | 任意 |
| 即位回数 | `reignSummary.reignCount` | 任意 |
| 確度 | `verification.confidence` ほか | 任意 |

**王朝データ探索（政権 87行 / 時代 11行のトグル）の列**

| 列 | 算出 |
|---|---|
| 政権名（`regimes[].label`）／時代名 | catalogs |
| 時代 | `regimes[].eraId` |
| 性格 | `regimes[].category`（正統 / 並立 / 反乱・自称）|
| 年幅 | `regimes[].startYear`〜`endYear`（**表示用のヒント。入れ子・重複する**）|
| 皇帝数 | 所属人数 |
| 平均在位年数 / 最長 / 最短 | 所属皇帝の集計 |
| 平均没年齢 | 値のある人だけで平均（**列見出しに n を出す**）|
| 死因の内訳 | 8カテゴリの積み上げバー（幅100%の1セル）|
| 歴代調査済み | `regimes[].dynastyOrderSurveyed`（**true は 87政権中36件のみ**。false の政権では「第N代」を出さない）|

集計列は**必ず母数を併記**する（人数1〜2の政権が多数あり、平均値が単独では誤読される）。

**「第N代」は既定では画面に出さない。** `reigns[].dynastyOrder` は **365人中204人が null**（未調査の政権）で、出せる人のほうが少ない。出す場合は `dynastyOrderSurveyed: true` の政権に限り、null を「初代」等に推論しない。

### 7.12 解説テキスト枠（`ProseSlot`）— **場所だけ用意する**

表とグラフだけのページは検索エンジンに評価されにくいため、**全画面に散文（地の文）の置き場所を用意する**。ただし**本文はまだ書かない**。デザイン段階では想定文字数のダミー日本語で枠を作る（`lorem ipsum` は使わない。全角の日本語ダミーで行数・折り返しを確認する）。

**3つの型**

| 型 | 位置 | 想定 | 見出し |
|---|---|---|---|
| `lead` | `h1` の直後 | 1段落・**80〜120字** | なし |
| `section` | ページ最下部（最後のデータブロックの後・フッターの前）| **400〜800字**・段落2〜4 | `h2` 1〜2本 |
| `person-summary` | 個別ページ（7.6 の2番目）| **200〜400字** | `h2`「人物の概要」|

**見た目**

- 本文幅は **38em 上限**（4章の規則）。`body` トークン、`--ink`。
- カードにしない。地は `--paper` のまま、上に `--line` の細罫を1本引いて「読み物の領域」であることを示す程度に留める（データ面と質を変える）。
- 段落間は `--sp-4`。見出しの前は `--sp-6`。

**規範**

- **SSR で出す。遅延マウント・折りたたみ・「続きを読む」の内側に入れない**（クローラが読めなくなる）。
- 見出しレベルを飛ばさない。`lead` に見出しを付けない（`h1` の直後なので）。
- **グラフ・表を見ればわかることを書かない**（本文執筆時の規約。デザイン段階のダミーもこの前提で長さを決める）。
  - 書かない: 「最長は康熙帝の61.9年です」「病死が最も多く161人です」のような、同じ画面に出ている数値の言い換え。
  - 書く: 収録基準（誰を365人に数えたか）、数え方の前提（数え年・在位日数の算出）、出典の性質（正史の本紀・原文引用）、データの限界（肖像150件・没年齢269件）、用語の定義（政権と王朝、正規と対立）、その画面の使い方。
- 文中から関連画面・個別ページへの**文脈内リンク**を張れるようにする（ダミーでもリンクを2〜3本含めた状態で設計する）。

### 7.13 空・欠損・不明の表現

| 状況 | 表示 |
|---|---|
| 肖像なし（215人） | モノグラム（7.3） |
| `ages.deathAge` が null（96人）| 「調査済み・不明」＋ `--ink-muted`。「—」だけにしない |
| `deathCause.category` = `unknown` / `disputed` | 灰のチップ。`disputed` は「諸説あり」と明記し `note` を必ず併置 |
| `datePrecision` が `year` / `month` | 日付を「620年」「620年3月」と精度どおりに出す。**日を捏造しない** |
| `exactDays` が null | 「約 N日」。断定形で出さない |

---

## 8. レイアウト・レスポンシブ

### デスクトップ（≥1024px）

- 左に**固定サイドバー 256px**（縦スクロール独立）。コンテンツ最大幅 **1440px**・センタリング。
- サイドバー上部にブランド（印章＋「中国皇帝統計」Serif）、その下に**常設のインクリメンタル検索**、その下にナビゲーション。
- **ナビは下記6項目のフラットな一列**。入れ子・アコーディオンは作らない（グラフごとの下位項目を増やさないため）。現在地は `--seal` の左罫 3px ＋ 太字で示す。
- **フッター**（全ページ共通・コンテンツ最下部）: **細い1行**。高さ 40px 前後（縦パディング `--sp-3`）、上に `--line` の細罫1本、`caption` トークン（12px・`--ink-muted`）、地は `--paper`。

  ```
  ────────────────────────────────────────────────────────────
  データ CC BY 4.0 / コード MIT　·　このサイトについて　·　GitHub
  ```

  **ライセンスは1行で書ききる**。ロゴ・リンク集・複数カラムのフッターにしない。「このサイトについて」への導線はここだけ（サイドバーには置かない）。狭い画面では中黒区切りのまま折り返してよいが、2行を超えない。

### モバイル（<1024px）

- 上部にコンパクトなブランドヘッダー（高さ 56px・スクロールで縮まない）。
- **下部固定ナビはサイドバーと同じ 6項目・同じ語**（各 44px 以上のタップ領域・アイコン＋ラベル 10px）。省略形を作らず、必要なら2行に折り返して同じラベルを出す:

  `概要ダッシュボード` / `皇帝一覧` / `皇帝データ探索` / `王朝データ探索` / `通史年表` / `系譜・家系図`

  ドロワーやハンバーガーは置かない（項目が6つしかないため）。「このサイトについて」はフッターからのみ。
- 下部ナビの高さぶん `padding-bottom: calc(56px + env(safe-area-inset-bottom))` を本文に確保する（最後の行が隠れない）。
- **フッターを下部固定ナビの裏に敷かない。** フッターは通常フローのままコンテンツ末尾に置き、上の余白の内側（＝ナビの上）に来るようにする。`/about` への導線はこのフッターのリンクだけなので、ナビに隠れると到達不能になる。

### 情報設計（サイト全体）— **セクションはこの6つで固定**

**指標ごと・グラフごとにページを増やさない。** グラフはすべて概要ダッシュボードに集約し、指標セレクタ（7.5）と分類セレクタ（7.8）で切り替える。ここに無いページを提案・追加しないこと。

| # | セクション | パス | 中身 |
|---|---|---|---|
| 1 | 概要ダッシュボード | `/` | KPI ＋ ランキング（指標切替）＋ 内訳（分類切替）＋ フィルタ ＋ ドリルダウン。**サイトのグラフはすべてここ** |
| 2 | 皇帝一覧 | `/emperors`・`/emperors/[id]` | 365人のカードグリッド（眺める）／ 個別ページ |
| 3 | 皇帝データ探索 | `/data/emperors` | 365行の表（比べる・並べ替える）。7.11 |
| 4 | 王朝データ探索 | `/data/dynasties` | 政権87行 / 時代11行の集計表。7.11 |
| 5 | 通史年表 | `/timeline` | **今回は箱だけ**（後述） |
| 6 | 系譜・家系図 | `/kinship` | **今回は箱だけ**（後述） |
| — | このサイトについて | `/about` | 収録基準 / 出典方針 / ライセンス / 免責。**サイドバーに置かず、全ページ共通フッターからのみ** |

「探索」はデータを表形式で掘る画面の呼称（英語の Discovery に当たる）。サイドバーのラベルは `皇帝データ探索` / `王朝データ探索`、見出しでは補足として「並べ替え・絞り込みのできる表」と添える。

**解説テキスト枠の配置（7.12・本文は後で書くので枠だけ）**

| 画面 | `lead`（h1直下・80〜120字）| `section`（最下部・400〜800字）| その他 |
|---|---|---|---|
| 概要ダッシュボード | ✅ | ✅ | — |
| 皇帝一覧 | ✅ | ✅ | — |
| 皇帝データ探索 | ✅ | ✅（列の定義・数え方の注意）| — |
| 王朝データ探索 | ✅ | ✅（政権と王朝の違い・集計の前提）| — |
| 皇帝の個別ページ | — | — | `person-summary` 200〜400字（7.6 の2番目）|
| 通史年表・系譜（箱だけ）| ✅ | — | 準備中の説明がこの枠を兼ねる |

**「箱だけ用意」の2画面（`/timeline`・`/kinship`）**

- ナビには**通常の項目として並べる**（グレーアウトも disabled もしない）。
- ページを開くと、そのセクションが何を見せる場所なのかを 1〜2行で説明し、**準備中であることを明示**する。中身のデータは出さない。
- 準備中の見せ方も本デザインの一部として設計する（余白だらけの白紙にしない）。他セクションへ戻る導線を必ず置く。
- レイアウトの枠（ヘッダー・パンくず・タイトル・本文領域）は本実装と同じものを使い、あとで中身だけ差し替えられる形にする。

---

## 9. アクセシビリティ（WCAG 2.1 AA）

- 本文・見出しは 4.5:1 以上（3章の値で担保済み）。非テキスト（チャートの塗り・境界・アイコン）は 3:1 以上。
- **色のみで意味を伝えない**（P2）。凡例テキスト、パターン、直接ラベルのいずれかを必ず併用。
- フォーカスは `outline: 2px solid var(--focus); outline-offset: 2px`。**`outline: none` の単独指定を禁止**。
- タップターゲットは最小 44×44px。
- 見出しは `h1 → h2 → h3` を飛ばさない。ページに `h1` はちょうど1つ。
- ダイアログは `role="dialog" aria-modal="true"`、フォーカストラップ、`Esc` で閉じる、閉じたら開いた要素へフォーカスを戻す。
- チャートは `figure` ＋ `figcaption`（見えている要約）＋ 7.7 の併置テキスト表で表現し、svg / canvas 本体は `aria-hidden="true"`。**`role="img"` ＋要約 `aria-label` は使わない**（下の「`aria-label` に説明を足さない」と衝突し、読み上げも二重になる）。
- `aria-label` に説明を足さない（読み上げと表示テキストを食い違わせない）。ラベルは見えているテキストと一致させる。
- キーボードのみで、一覧のフィルタ→カード→個別ページ→戻る、が完結すること。

---

## 10. パフォーマンス規範

- 皇帝一覧の365件は **全件を実カード（`<a>`）として SSR で出す**。仮想化して DOM から消さない。描画コストは `content-visibility: auto` ＋ `contain-intrinsic-size`（カードの実寸）＋ 画像 `loading="lazy"` で抑える（P5）。
- **視覚的に隠した365件のリンクリストは作らない。** 実カード自体がクロール可能なリンクなので重複であり、隠しリンクの塊は評価されにくい。
- 画像は `loading="lazy"` ＋ `width`/`height` 明示（3:4）。**CLS 対策として必ずアスペクト比を予約**する。
- 一覧グリッドには **320px 幅相当のサムネイルを別途用意して配信する**（原本は 360×480）。150枚を原寸で並べると転送量が跳ねる。`srcset` で一覧＝サムネ／個別ページ＝原寸に出し分ける。
- ぼかし面は同時 10 要素まで（6.2）。アニメーションは `transform` / `opacity` のみ（`top`/`left`/`width` を動かさない）。
- スクロール連動アニメーションを新規に足さない。
- 初期表示に必要のないチャートは遅延マウントする。ただし **7.5 の既定の上位10件表・7.7 の併置テキスト表・7.11 の先頭50行・7.12 の解説テキスト・一覧の365カードは遅延の外**。
- 目標: LCP < 2.5s / CLS < 0.1 / TBT < 300ms（実機）。

---

## 11. 表記・用語ルール

- **年齢はすべて数え年**。「満」ではない。年齢を出す箇所には必ず `caption` で「数え年」と書く。
- **在位年数は `displayYears`**、在位日数は `approxDays`（3桁区切り）。「61.9年（22,597日）」の順で併記。`displayYears` はデータ上 小数1桁（128人）と2桁（237人）が混在するので、**表示側で必ず小数第1位に丸める**（例: 61.91 → 61.9）。
- 紀元前は「前221年」と書く（`startYear: -221`）。**`-221年` と出さない**。
- **`researchSection` は調査ブロック名であり、画面に出してはいけない**（「秦（始皇帝以降）」のような調査都合の区分名）。時代は `eras[].label`、政権は `regimes[].label` を使う。
- 政権は `regimes[].label`（曖昧性のない表示名。例「梁（蕭梁）」）を使う。`name`（「梁」）は同名が複数あるので単独で出さない。
- 「王朝」という語は UI ラベルとして使ってよいが、データ上の単位は「政権（regime）」であることを `/about` で説明する。
- 死因・即位経路などの**日本語ラベルを UI にハードコードしない**。必ず `meta.catalogs.enums.<field>` から引く（11章の詳細は CLAUDE_DESIGN_PROMPT.md のデータ契約を参照）。

---

## 12. 禁止事項（違反したら実装のやり直し）

1. データにない値を UI で計算し直して「事実」として出す（例: 肖像の生成、生没年の推定、`exactDays` が null の人の日数を勝手に確定させる）。
2. 日本語ラベルのハードコード（`catalogs` を引かない）。
3. 削除済みフィールドの使用: **`dynasty`（オブジェクト全体）・`flags.selfProclaimed`・`accessionRoute.category`** — v3 で廃止済み。存在しない。
4. `--gold` を文字色・細罫・単独の情報表現に使う（2.18:1）。
5. 皇帝カードグリッドへの `backdrop-filter` 適用。
6. 皇帝一覧の365件を仮想化して DOM から消す／SEO 用の隠しリンクリストで代替する（全件を実カードとして SSR で出し、`content-visibility` で描画だけ抑える。10章）。
7. 母数（n）や単位のない平均値の表示。
8. 色だけに依存した凡例。
9. 肖像のトリミング・拡大・スタイル加工（セピア、モノクロ化を含む）。
10. `researchSection` の画面表示。
11. **8章の6セクション以外のページを作る**（指標ごと・グラフごとにページを増やさない。グラフは概要ダッシュボードに集約し、セレクタで切り替える）。
12. `/timeline`・`/kinship` に中身のデータを載せる（今回は枠と「準備中」の表示だけ）。
13. 解説テキスト枠（7.12）を折りたたみ・「続きを読む」・遅延マウントの内側に入れる。
14. 解説テキストに、同じ画面のグラフ・表を見ればわかる数値の言い換えを書く。
15. フッターを多段組み・リンク集にする（細い1行・ライセンス1行が規定）。モバイルでフッターを下部固定ナビの裏に敷く。
16. チャートに `role="img"` ＋要約の `aria-label` を付ける（`figure`＋`figcaption`＋併置テキスト表が正。7.7・9章）。
17. `data/emperors.json` に無いフィールドを前提にした UI を作る（かな読み検索、英語ラベル、`cbdb` など）。
18. モノグラムに**諱の先頭1字**を使う（末尾1字が規定。7.3）。
