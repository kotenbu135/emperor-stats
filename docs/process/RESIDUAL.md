# 残量表

**同じ穴から出てきた走査結果を、新しい Issue にせずここへ足す**ための表です。

2026-08-02〜08-03 に、`data/emperors.json` の1件の訂正から横展開が連鎖して Issue が
28本立ち（#40 起点）、うち6本が「同じ穴が別の場所から顔を出しただけ」でした
（#55・#56・#57・#62 → 親 Issue #68 へ統合）。Issue は**片づけ方が1つに決まる問題**を
持つ器で、「母集団 N のうち M 件が未読」という**量**を持つには向きません。量は本数ではなく
行数で増えるべきなので、この表を置きます。

規則は `docs/process/RULES.yml` の `R-RESIDUAL-TABLE`。`gh issue create` は
`.claude/hooks/guard.py` が一度止めます（理由を書けば通ります）。

## 行を足すとき・立てないとき

**ここへ足す** — 既にある系統の走査結果・横展開で出た同型の疑い・標本監査で測った取りこぼし率。
つまり「**どう直すかは分かっていて、残りが何件あるかの話**」。

**Issue を立てる** — 直し方が決まっていないもの。判断・定義・設計が要るもの。
たとえば #58（唐太宗の在位開始日を動かすか）は原典3箇所を読み終えたうえでの**内容の判断**で、
量ではないので Issue が正しい器です。#66（改元の `date` は詔の日か元号開始日か）も定義の問題です。

**行を消すとき** — 母集団を読み切ったか、検出器が乾いた（K 回続けて新しい該当が出ない）とき。
**「訂正が終わった」ではありません** — 未読が残っているなら行は残します。

## 書き方

- **未読・推定は測った数で書く。** 走査していないなら「未走査」と書く（0件と未走査は違う）
- **absent 側（機械が何も見つけなかった側）は、種つき標本を原典で読んで取りこぼし率を測ってから
  推定に掛ける**（規則 `R-SCREEN-FIRST`）。測っていないなら「未測定」
- **記録の列には `data/screenings.json` のキーか、件数を出すスクリプト名を書く。**
  「どこを見ればこの数字が引き直せるか」が無い行は、次に読む人が数え直せない

## 表

**数字は `記録` 列から引き直したもの**（2026-08-03 時点。#68 本文のスナップショットではない）。
`screenings.json` の行は `python3 scripts/check_screenings.py --scope` が同じ数を出す。

| 系統 | 母集団 | 確定した欠陥 | 未読・推定 | 記録 |
|---|---:|---:|---|---|
| 親征イベントの終期 | 291 | 24（#56 で訂正。読了42件中18件は現行が正しかった） | 要読解95のうち**53件が未読** ＋ absent 196 × 取りこぼし率50%（95%区間 16〜84%）→ **31〜165** | `screenings.json` `campaign-span-issue56` |
| 日精度 ISO と原文干支の食い違い | 1,336（event-date-field） | **79件（#55 で訂正・push 済み）** ＋2件（#57） | 要読解29（#56 の訂正で母集団が 1307→1336 へ増えた分・**標本監査はこの増分を覆っていない**） ＋ absent 1,307 × 取りこぼし率7%（95%区間 1〜23%）→ **13〜300** | `screenings.json` `event-date-ganzhi-issue55` |
| 旧暦月番号の太陽暦欄への直書き | 3,203（月精度） | 5件（#56 で訂正） | #62 の未訂正9件（下表・2026-08-03 に現値を確認済みで**#55 の訂正は届いていない**）＋ **母集団は未走査** | 未作成 |
| 訂正済みフィールドの兄弟に残る旧暦直書き | 未確定 | — | **走査していない。**#67 は 2026-08-03 に閉じてこの行へ吸収した（下の「2026-08-03 の決定」） | 未作成 |
| 年精度の埋め草（`-01-01`／`-12-31`） | 1,412（年精度） | — | 未走査（#56 の標本監査で晋恵帝・後秦姚興がこの形だった） | 未作成 |
| `*Raw`／`conversion` の充足 | 6,026 | — | **0件**（`verify_calendar.py` が `B5=0`。器は在るが1件も検査していない） | `verify_calendar.py` の件数出力 |

### 旧暦月番号の直書きが疑われる9件（#62・2026-08-03 に現値を確認）

`beiqi-wenxuandi.personalCampaignCount` の #3 start・#4 両端・#6 両端・#7 両端（計7値）／
`qing-shengzu` #1 両端・#2 end（4値）／`xixia-xiangzong` #0（年が嘉定二年とずれる疑い）／
`houqin-yaoxing` #6（十六国春秋 後秦録は弘始十一年七月に置く）／`suimo-lizitong` #1（起点に原文の裏付けが無い）／
`hou-han-guangwudi` #4（終期を九月乙卯「车驾还宫」で日精度化できる）／`xixia-jingzong` #0・#5／
`jin-taizu` #4（`datePrecision:"month"` のまま `endDate` が日精度）。**いずれも原典の根拠は #68 の6節にある。**

**2026-08-03 ユーザー決定: この9件は訂正せずアーカイブへ入れる。** 主張範囲を絞る移行（下）で、
これらの多くは「主張しない月日」に落ちてアーカイブ側へ移る。**誤りと分かったまま移る**ので、
後でアーカイブから精度を戻すときは、この節を先に読むこと（#67 もここへ吸収して閉じた）。

## 2026-08-03 の決定（Issue #69）— この表の日付系の行は移行後に畳める

**events の日付について、データセットが主張するのは「年精度 ＋ 在位境界年の月日」だけにする**と
決まった（#69・ユーザー決定）。丸めた月日は消さず `data/internal/event-date-archive.json` へ退避し、
**内部側はこれ以上精度を追求しない**。あわせて**埋め草を廃止**し、保存値の深さそのものを主張にする
（年は `"1211"`・月は `"1211-05"`・日は `"1211-05-07"`）。

移行が終わったら、上の表の日付系5行は次のように畳める（`b02cd14` 時点の実測）。**移行前に数字を
書き換えないこと** — データが動いていない段階で表を先に直すと、この表が最初の版で陳腐化したのと同じ形になる。

| 行 | いまの母集団 | 移行後 |
|---|---:|---:|
| 親征イベントの終期 | 291 | 主張する終期は **60値** |
| 日精度 ISO と原文干支 | 1,336 | 日精度の値 1,351 → **582値** |
| 旧暦月番号の直書き | 3,203 | 月精度の値 4,933 → **1,023値** |
| 訂正済みフィールドの兄弟 | 未確定 | 上の2行に吸収 |
| 年精度の埋め草 | 1,412 | **0**（形が主張になるので区別が要らなくなる） |
| `*Raw`／`conversion` の充足 | 6,026 | **1,173件**（月日を主張する event） |

親 Issue は **#68**（`events[]` の日付に原表記と換算を行き渡らせる）。畳んだ #55・#56・#57・#62 の
working knowledge — 検出器の件数・検出力・未訂正候補の一覧・原典の根拠 — は #68 の6節にあります。

**この表の最初の版は、#68 のスナップショットをそのまま写して1行が陳腐化していました**
（#55 は「要読解100件が未読」と書いたが、実際には並行セッションが読み切って79件を訂正済みだった）。
**行を読むときも書くときも、まず `記録` 列のコマンドを走らせて数字を引き直してください。**

## この表の外の残量（それぞれ既存 Issue が持つ）

| 系統 | 母集団 | 未読・推定 | Issue |
|---|---:|---|---|
| 名前フィールド（廟号・諡号） | 558 person-field | 要読解171 ＋ 諡号 absent 205 × 67% → 約137 | #37 |

**引用の行（母集団 6,504・未解決 234 ＋ 57）は 2026-08-03 に外した。** #38 は完走しないと決まり
（`docs/schema/CLAIM_SCOPE_PLAN_2026-08-03.md` の6節）、散文の中の引用は配布物の主張ではなくなる。
代わりに置く残量は「**構造化引用の床を満たす転記**」で、移行が始まったらこの表に行を足す
（`deathCause` 365 ＋ `accessionRoute` 365 ＋ 8つの `count` 2,920容器。いまの構造化引用は 780件＝全体の11.2%）。
