# 肖像画収集の実現可能性調査（2026-07-17）

サイトに皇帝の肖像画を掲載する構想について、**実際に画像が取得できるか**をdata/emperors.json収録の364名全員（当時の全収録数。2026-07-20追加の唐哀帝は本調査の対象に含まれない）で確認した記録。**画像の実装は完了済み**（サイトの皇帝一覧カード・出典一覧で使用中。2026-07-21 に全数目視QA実施、経緯は本ドキュメント後半参照）。

## 経緯

1. 参考にできる先行事例として「鳥人間 中国史三昧」（YouTubeチャンネル）の画像の出典を調査したが、動画説明欄・チャンネルAbout・Xいずれにも出典表記はなく（BGMクレジットのみ）、モデルにはできないと判断
2. 出典・ライセンスが明示されている一次ソース（Wikimedia Commons／台北故宮オープンデータ）を使う方針に決定
3. 実際に364名分取得できるか確認する過程で、Wikimedia API（`action=opensearch`・REST `page/summary`）への逐次リクエストが429（レート制限）を頻発させる事態が発生。過剰なリクエスト量だったことを踏まえ、**ライブAPI呼び出しをすべて中止**し、Wikimedia公式のデータダンプ（一括ダウンロード＋完全ローカル処理）に方式を切り替えた

## 採用した方式：データダンプによるオフライン照合

zh.wikipedia のダンプ（`dumps.wikimedia.org/zhwiki/latest/`、2026-07-06時点スナップショット）3ファイル（合計約340MB）をダウンロードし、以降は**追加のネットワークアクセスなし**でローカル処理のみで照合した。

| ファイル | 内容 | サイズ |
|---|---|---|
| `page.sql.gz` | ページID⇔タイトル対応表 | 約269MB |
| `page_props.sql.gz` | ページの`page_image_free`（自由ライセンスのリード画像の有無） | 約51MB |
| `redirect.sql.gz` | リダイレクト解決 | 約18.6MB |

### 処理の流れ

1. `data/emperors.json`の`name`（personalName/commonName/aliases/templeName/posthumousName）と`dynasty.name`から候補タイトルを機械的に生成（王朝名プレフィックス付与、括弧内別名の抽出など）
2. **日本語新字体→中国語繁体字/簡体字の変換**を候補に適用（例: 「呉三桂」→「吳三桂」/「吴三桂」）。データ内の人名表記が日本語入力由来の新字体であるため、zh.wikipediaのタイトル（繁体字/簡体字）と直接一致しないケースが多数あり、この変換により取りこぼしが18件→0件に減少
3. 候補タイトルを`page.sql`と照合し、リダイレクトなら`redirect.sql`で解決先タイトルを取得、最終的なページIDを特定
4. ページIDが`page_props.sql`の`page_image_free`を持つか判定

候補生成・王朝バリアント表などの機械的な変換ロジックのみで、**人物ごとの歴史的判定は一切行っていない**（[CONSTRAINTS.md](../process/CONSTRAINTS.md)の「計算補助スクリプトはOK」の範囲内）。

## 結果

| 区分 | 人数 |
|---|---|
| **自由ライセンス画像あり（`page_image_free`）（機械照合時点）** | 168人 / 364人（約46%） |
| **うち目視確認の結果、実際に肖像画だったもの** | 155人 |
| **うち肖像画ではなかったもの（目視確認で除外）** | 13人 |
| **うちCC BY-SA 4.0ライセンスのため方針により除外（下記参照）** | **2人** |
| （2026-07-17 時点の確定数） | 153人 |
| **うち目視QAで除外（2026-07-21・下記参照）** | **3人** |
| **うちカード表示での見直しで除外（2026-07-31・下記参照）** | **6人** |
| **個別発見による追加（2026-08-01・下記参照）** | **＋1人** |
| **再探索による追加（2026-08-02・下記参照）** | **＋9人** |
| **別クラス調査の副産物による追加（2026-08-05・下記参照）** | **＋1人** |
| **現在の確定数** | **155人** |
| 画像なし（本調査の除外分を含む・現在） | 209人 |
| 記事自体が見つからない | 0人 |

- 全364名がzh.wikipediaの何らかの記事にマッチ（取りこぼしなし）
- 155名分の詳細（照合したタイトル・画像ファイル名）は [`portraits-candidates.json`](./portraits-candidates.json) に記録
- 残り209名は本調査では**「画像なし」として一旦保留**（`portraits-candidates.json` の `noImageIds` は、2026-07-20 に収録した唐哀帝を 2026-08-02 に足したため210件。合計が365人に揃う）

### 目視確認で除外した13名（2026-07-17）

機械的な照合（`page_image_free`の有無）だけでは、**画像が実際に肖像画かどうかまでは判定できない**。ダウンロード後にサンプルを実際に表示したところ、`beiwei-xiandi`（北魏献文帝）の画像が肖像画ではなく雲岡石窟の風景写真だったことが発覚。ユーザーが13名分を目視確認し、以下がいずれも肖像画ではない（風景・石窟・仏像・建造物等の写真）と判定、`confirmed`から除外し画像ファイルも削除・`noImageIds`に移動した。

`beiwei-houfeidi-yuanlang` `beiwei-mingyuandi` `beiwei-wenchengdi` `beiwei-xiandi` `beiwei-xiaowendi` `han-liuhe` `houzhao-shihu` `jin-taizong` `liang-xiaoji` `liang-xiaozhuang` `liao-taizu` `shun-lichengzheng` `xiliao-dezong`

**このため、`page_image_free`による機械的な一次スクリーニングは有効だが、最終的な採否は必ず人間の目視確認を経ること**（[CONSTRAINTS.md](../process/CONSTRAINTS.md)の「判定そのものをスクリプトに代行させない」の精神と整合）。

### CC BY-SA 4.0ライセンスを方針により除外（2026-07-17）

目視確認を経て残った155件のうち、CC BY-SA 4.0（要クレジット表記）ライセンスだった2件（`beiwei-daowudi`、`shiguo-qianshu-wangjian`）は、**「CC BY-SA系ライセンスは使用しない」という方針決定**により追加で除外した。画像ファイル・`manifest.json`のエントリを削除し、`portraits-candidates.json`では`noImageIds`へ移動、除外理由は`excludedLicense`フィールドに記録。**この時点の確定数は153件**、全件Public domain/CC0のみとなり、要クレジット表記の対応は不要になった（その後2026-07-21の目視QAで150件、2026-07-31のカード表示での見直しで144件、2026-08-01の個別発見による追加で145件、2026-08-02の再探索で154件、2026-08-05の別クラス調査の副産物で155件）。

## 肖像マッピングQA（2026-07-21）— 153件の全数目視確認

2026-07-17 時点の153件について、「割り当てられた画像が本当にその皇帝か」を全件確認した。結果、**1件を差し替え・3件を除外し、確定数は153→150件**になった。

### 機械検出（5種）

いずれもスクリプトによる列挙のみで、採否の判定は目視で行った（[CONSTRAINTS.md](../process/CONSTRAINTS.md)の「判定そのものをスクリプトに代行させない」に整合）。

1. `sourceFilename` / `commonsPageUrl` の重複 → 2ペア検出
2. 画像バイナリのMD5重複 → 同じ2ペア
3. ファイル名に「別の皇帝の諱」が含まれる → 1件
4. `matchedWikiTitle` が他の皇帝を指す → 1件（「宋太宗」の衝突）
5. `matchedWikiTitle` の王朝接頭辞と記録の王朝の不一致 → 誤検出のみ

**1・2 はそのままCIの「画像URL重複」チェックに転用できる**（[../../task.md](../../task.md) 項目3-3）。

### 全数目視確認

153件すべての画像を実見し、疑義が出たものは先行判定を伏せずに「覆してよい」と指示した別の確認者が再検証した（153件が重複なく1回ずつ判定されたことは機械確認済み）。既知の誤り2件を伏せたまま流したところ両方とも検出されたため、検出プロセス自体の対照テストは通っている。

### 対応（4件）

| id | 内容 | 対応 |
|---|---|---|
| `houyan-murongchui` | 後燕・成武帝（慕容垂）に、題字「蜀主李雄」の図＝成漢武帝 李雄の絵が割当。諡号「成武帝」の衝突による誤マッチ | 同じ『繡像三國演義續編』の**題字「慕容垂」の図に差し替え**（PD） |
| `liu-song-mingdi` | 劉宋・明帝（劉彧）に、展脚幞頭＋白袍の宋代皇帝常服の立像＝北宋太宗の肖像が割当。廟号「太宗」＋王朝名「宋」で「宋太宗」に衝突 | Commonsに劉彧のPD/CC0画像が見つからないため**肖像なしへ** |
| `han-aidi` | 前漢・哀帝に陳洪綬『博古葉子』の断袖故事図。哀帝も画中にいるが札の主題・題名は董賢で、どの人物が哀帝かを画像から特定できない | 肖像画ではなく**場面図の切り出しのため除外** |
| `tang-jingzong` | 唐・敬宗に清初彩絵『帝鑑図説』の撃鞠（ポロ）場面の切り抜き。画集の体裁上は皇帝本人だが題字がなく複数騎の場面図 | 同上（**場面図の切り出しのため除外**） |

### この確認で閉じきれない残存リスク

**「全件正しいと確認できた」わけではない**。今回検出できた2件はどちらも「バイナリ重複＋題字が別人名 or 明白な別王朝の服飾」という易しいクラスで、次のクラスは原理的に目視では拾えない：

- 単独人物・その時代として正しい服飾・題字なしで、**同時代の別人**が描かれているケース。顔の正解像が存在しないため妥当に見える。

残存リスクを潰すには外部の正解データとの突き合わせ（Wikidata P18 / Wikipedia記事のリード画像）が必要で、目視を増やしても閉じない。[../../task.md](../../task.md) 項目2-5（Wikidata QID紐付け）に合流させるのが筋。

## 顔の位置（`focusY`）とカード表示での見直し（2026-07-31）

皇帝一覧のカードを3:4にした結果、カード内の肖像枠は実体（360×480＝3:4）より横長のほぼ正方形になり、
`object-cover` が縦を切るようになった。**それまでの「上寄せ」固定は誤り**で、題字が上端に入る版本
（三才圖會の「漢武帝像」など）や顔が中央にある元の御容では、題字と余白が枠を占めて顔が下半分に沈んでいた。

そこで `manifest.json` の各エントリに **`focusY`（顔の中心が画像の上端から何割の位置にあるか・0〜1）** を持たせ、
サイト側で `object-position` へ変換している（式と丸めの意味は `site/src/components/emperors/portrait.tsx` の
`focusObjectPositionY`）。**値は144枚を1枚ずつ実見して入れたもの**（2026-08-01以降の追加分も同じ手順で1枚ずつ読んでいる）で、10%刻みの目盛を重ねた
コンタクトシートから読み取った。実体（jpg/webp）は切り直していない — 枠が実体より横長のあいだ
`object-cover` は縦だけを切るので、**どこを切るかを決めさえすれば切り直した場合と同じ絵が出る**ため。

- `focusY` が約 **0.26 以下**の肖像（立像・全身像の多く）は変換後が0に丸まり、従来どおりの上寄せになる。
  つまりこの仕組みは**顔が下に沈んでいる肖像だけを動かす**
- 約 **0.51 以上**の肖像は下寄せでも狙いに届かない（webp の下端が既に切れているため）。
  該当が出たら jpg から切り直すのが筋だが、155枚では発生していない（2026-08-02 に足した南宋の御容3枚が0.52〜0.53で最も下）

### カード表示での見直しによる除外（6件）

同じ作業の中で、144＋6件すべてを**カードに出た状態で**見直した。判定基準は2026-07-21のQAと同じ
「どの人物が該当人物かを画像から特定できない」で、**確定数は150→144件**になった。
題字で本人が特定できるもの（`jin-huidi`・`shiguo-beihan-liujun`・`wudai-houtang-mingzong` など）と、
帝王図巻のように主人公が明らかに大きく描かれているもの（`chen-feidi`・`han-chengdi` など）は残している。

| id | 内容 | 対応 |
|---|---|---|
| `beiqi-wenxuandi` | 建物内の場面図。該当人物が小さく後ろ姿寄りで、画像から本人と特定できない | 除外（場面図） |
| `han-zhaodi` | ほぼ同じ大きさの3人が並ぶ群像。どれが昭帝かを特定できない | 除外（群像） |
| `tang-wuzetian` | 侍者を含む群像。どれが武則天かを特定できない | 除外（群像） |
| `wei-caofang` | 馬上の人物を含む版本の場面図。該当人物を特定できない | 除外（場面図） |
| `wei-caomao` | 同じ大きさの人物が並ぶ版本の場面図。該当人物を特定できない | 除外（場面図） |
| `yuan-huizong` | 剥落・退色が進み、像として人物が判読できない | 除外（判読不能） |

### 反映先（肖像画を増減するときの手順）

肖像画は `data/emperors.json` には一切持たせていない（ファイルの存在で決まる）ため、**データ項目の訂正ではなくサイトのアセット修正**として扱う。`meta.status` は変更しない。

1. `data/images/portraits/{id}.jpg`（幅500pxサムネイル）。**除外するときはこのファイル自体を消す** — manifest からエントリを外すだけだと `python3 scripts/validate_emperors.py` が「manifest に載っていないファイル」でエラーになり、**GitHub Pages のデプロイが gate で止まる**（2026-07-31 の6件除外はここを踏み、`main` を push した時点で初めて落ちた。サイト側のビルドは通るので site だけ検証しても見つからない）
2. `data/images/portraits/manifest.json` のエントリ。**`focusY`（顔の位置）を必ず入れる** — 肖像がある全員に無いとサイトのビルドが落ちる（`site/src/lib/emperors.ts`）。入れ忘れを黙って上寄せに落とすと顔が下半分に沈む
3. `docs/site-design/mockups/card-preview/{id}.webp` ← **サイトが実際に配信する実体はこちら**。360×480に`cover`＋上寄せでクロップ、WebP quality 65（**カードのどこを切るかは実体ではなく `focusY` で決まる**ので、この生成手順は変えない）
4. `site/public/portraits/` は `site/scripts/sync-portraits.mjs` が`prebuild`で同期する（同期元から消えた肖像は public 側からも削除される）
5. `docs/site-design/portraits-candidates.json` の `confirmed` / `noImageIds` / 件数
6. 本ファイルの件数と、`docs/README.md` の件数・`SITE_DESIGN.md` の「7. 皇帝一覧」節の肖像/モノグラム表

`/about` の出典一覧は `manifest.json` から自動生成（`site/src/lib/emperors.ts` の `getPortraitCredits()`）のため、件数のハードコードはない。

## 個別発見による追加（2026-08-01）— `ming-yizong`（崇禎帝）

**確定数は144→145件**。ユーザーが ja.wikipedia「崇禎帝」記事のリード画像として肖像を見つけ、追加したもの。

| 項目 | 内容 |
|---|---|
| id | `ming-yizong`（明・毅宗＝崇禎帝／朱由検） |
| Commons | [File:明愍帝朱由检.jpg](https://commons.wikimedia.org/wiki/File:%E6%98%8E%E6%84%8D%E5%B8%9D%E6%9C%B1%E7%94%B1%E6%A3%80.jpg)（原寸 780×1784） |
| 出典 | 『繡像鐵冠圖』（1907年・上海書局／松滋山人）。ハーバード燕京図書館デジタル版 |
| ライセンス | **Public domain**（`AttributionRequired: false`・要クレジット表記なし） |
| `focusY` | **0.45** |

採否の根拠は2026-07-21・2026-07-31のQAと同じ基準で、**図の右上に題字「崇禎帝」があり、その真下に単独の人物が大きく描かれている**ため本人と特定できる（`jin-huidi`・`wudai-houtang-mingzong` と同じクラス）。左下に隣の人物の裾が写り込むが群像ではなく、カード枠（3:4のほぼ正方形）には入らないことを実際に切って確認した。

**この1件はダンプ照合（`page_image_free`）の産物ではない。** `ming-yizong` は2026-07-17の機械照合では画像なし側にいた（zh.wikipedia の記事にリード画像フラグが立っていなかった）。そのため `manifest.json` の `matchedWikiTitle` と `portraits-candidates.json` の `matchedTitle` は **`null`** にしてある — 照合で当てたタイトルが存在しないため。**「画像なし」219名の中には同様にCommons側には画像があるものが残っている**（PORTRAITS.md 末尾の「未確認・今後の注意点」で予告していたとおり）ので、見つかり次第この手順で足せる。**2026-08-02 にこの見込みを全数で検証し、9件を追加した**（下の「再探索による追加（2026-08-02）」節）。

取得は既存と同じく Commons のファイル名MD5からURLを算出して幅500pxサムネイルを直接取得（1リクエスト）、ライセンスは Commons Action API の `extmetadata` を1リクエストで確認した。

## 再探索による追加（2026-08-02）— 9件

**確定数は145→154件。** 2026-07-17 の機械照合は zh.wikipedia ダンプの `page_image_free` 一本だったため、
`ming-yizong` のように「Commons には画像があるがフラグが立っていない」人物が取りこぼされていた。
その見込みを、肖像なし220名について**4系統で全数検証**したもの。

| 系統 | リクエスト | 結果 |
|---|---:|---|
| Wikidata P18（`sources.wikidata` の QID・SPARQL 1本） | 1 | 220名中17名がヒット（多くは既除外と同じファイル） |
| ja/zh Wikipedia のリード画像（`prop=pageimages`・50件/リクエスト） | 10 | ja 25件・zh 21件 |
| ja/zh 記事本文中の全画像（`prop=images`） | 10 | 750件中、人名が一致したのは印璽・哀冊など3件のみで収穫なし |
| Commons のファイル名検索（`intitle:`・繁簡両方＋廟号/諡号のフォールバック） | 719 | 87名でヒットしたが、目視の結果**新規採用ゼロ**・差し替え候補3件のみ |
| Wikidata P373（Commons カテゴリ）＋カテゴリ列挙 | 28 | 残り209名のうちカテゴリを持つのは27名だけ。墓誌・壁画・場面図・群像のみで採用ゼロ |

**生産的だったのは上2系統**（P18 と ja/zh のリード画像）で、9件のうち7件がここから出ている。
Commons の全数掃きは719リクエストを費やして**新規採用ゼロ**で、`intitle:` は CJK に対して
部分一致で当たるため、ヒットの大半が同名の現代人だった（例: 沖帝「劉炳」に民国期の人物写真）。

| id | 皇帝 | Commons ファイル | ライセンス | `focusY` | 発見経路 |
|---|---|---|---|---:|---|
| `hou-han-lingdi` | 後漢・霊帝 | `汉灵帝 (cropped).png` | PD | 0.48 | P18 |
| `liang-yuandi` | 梁・元帝 | `Liang Xiao Yi.jpg` | PD | 0.27 | ja リード画像 |
| `liu-song-qianfeidi` | 宋・前廃帝 | `宋前廢帝.jpg` | PD | 0.40 | P18 |
| `nansong-duanzong` | 南宋・端宗 | `ZhaoShiDuanzong.png` | PD | 0.52 | P18 / ja |
| `nansong-gongdi` | 南宋・恭帝 | `Song Gongdi2.jpg` | PD | 0.53 | P18 / ja |
| `nansong-weiwang` | 南宋・衛王 | `Song Modi.jpg` | PD | 0.53 | P18 / ja |
| `qi-donghunhou` | 斉・東昏侯 | `Xiao Baojuan.jpg` | PD | 0.29 | ja リード画像 |
| `tang-wuzetian` | 周・則天大聖皇帝 | `武曌像.jpg` | PD | 0.51 | Commons 検索 |
| `shun-lichengzheng` | 順・永昌帝（李自成） | `李自成.jpg` | PD | 0.36 | Commons 検索 |

末尾2件は**除外済みからの差し替え**（`houyan-murongchui` と同じ扱い）。`tang-wuzetian` は
2026-07-31 に群像図で除外したものを絹本着色の単独上半身像へ、`shun-lichengzheng` は
2026-07-17 に非肖像（李自成行宮の建物写真）で除外したものを版本の単独立像へ置き換えた。

### 目視で落としたもの（判定の記録）

**機械照合の結果をそのまま採らないという原則は、この回でも効いた。**

| 候補 | 落とした理由 |
|---|---|
| `hou-han-shangdi` ← `劉隆（東漢演義評）.jpg` | 題字「劉隆」の成人武人像。殤帝劉隆は生後100日で即位し1歳で崩じており、後漢の武将 劉隆（慎侯）と判断 |
| `hou-han-chongdi` ← `劉炳.jpg`／`suimo-liqui` ← `李軌.jpg` | いずれも民国期の人物写真（「四川安縣 27歳」等の添え書きあり）。同名の別人 |
| `qi-wudi`・`qi-yulinwang`・`qi-hailingwang` ← `S 70942xx` | **2026年4月に投稿された現代の手描きイラスト**（self-published・CC0）。ライセンスは通るが歴史的肖像画ではない |
| `jin-xizong` ← `金熙宗半身像.jpg` | **金上京歷史博物館の胸像（現代の彫刻）の写真**。2026-07-17 に `liao-taizu`（耶律阿保機の銅像写真）を落としたのと同じクラス。CC BY 4.0 でもある |
| `hou-han-hedi` ← `漢和帝.png` | 題字「漢和帝」＋単独だが、図は版本ページの上部44%だけで残りが本文。3:4 に切っても**カード面の約1/3が縦組みの本文**になる |
| `yuan-huizong` ← `元順帝妥歡帖木兒…png` | 中央の人物は大きいが侍者を伴う群像。同じ回に「群像だから」と `tang-wuzetian` を差し替えており、基準が噛み合わない |
| `beiwei-yuanhao` ← `繡像三國演義續編 北海王乂.jpg` | 三国演義続編の司馬乂。称号「北海王」の一致のみ |
| `後漢通俗演義人物繡像圖` 22件（桓帝・少帝弁など） | いずれも人名の題字が付いた**5人並びの群像**。`han-zhaodi` を落とした基準と同じ |

**新しく足すべき判定基準はこれ**: `S 70942xx` と `金熙宗半身像` は、**ライセンスも人物特定も通るのに
「歴史的な肖像画ではない」**（現代の創作イラスト・現代の彫刻の写真）。既存の除外理由（非肖像画・
場面図・群像・判読不能・別人）のどれとも文言が違うので、次に照合するときはここを見る。

### 既存154件のヒントになる副産物: P18 との突合

PORTRAITS.md が「目視では閉じない残存リスク」として挙げていた**同時代の別人**の検出（この文書の
「この確認で閉じきれない残存リスク」節）は、Wikidata P18 との突合で一度だけ機械的に見られる。
2026-08-02 に既存145件で実施したところ、P18 を持つ119件のうち**33件が manifest と別ファイル**だったが、
目視した限りいずれも同一人物の別スキャン・別版で、**別人を指しているものは無かった**。
一覧は [`portrait-p18-mismatch-2026-08-02.json`](./portrait-p18-mismatch-2026-08-02.json)。
候補の全メタデータ（採用・不採用の別、Commons URL、ライセンス）は
[`portrait-candidates-2026-08-02.json`](./portrait-candidates-2026-08-02.json)。

## 別クラスの画像（拓本・碑刻）は入口ごと閉じた（2026-08-05・Issue #81 の3）

肖像なし211人の面を**肖像以外の画像**で埋められるかを測り、**A-2（拓本・碑刻）は成立しない**と結論した。
再提案しない。生データは [`rubbing-survey-2026-08-05.json`](./rubbing-survey-2026-08-05.json)（**2026-08-02 の
P373 列挙は「肖像として採用ゼロ」の一行しか残さず、そこに見えていた墓誌・壁画が何人ぶんだったかを
今回引き直す羽目になったので、今回は列挙結果をファイル名まで残してある**）。

| 入口 | 実測（肖像なし211人） |
|---|---|
| Wikidata P373（Commons カテゴリ） | **29人**（2026-08-02 の27人を再現） |
| Wikidata P119（埋葬地）→ そのカテゴリ | 28人 → **13人** |
| Wikidata P1442（墓の画像） | **1人** |
| 上の42カテゴリを列挙したファイル | 923件 |
| 拓本・墓誌のクラス側カテゴリ（`Epitaphs in China` ほか9件） | 341件 |

**1,264件を通して、皇帝本人の拓本・墓誌は3人ぶん4ファイルしか無い。** しかも:

- `beiwei-yuanye`（北魏・前廃帝 元曄）の墓誌＝**CC BY-SA 4.0**（洛陽博物館蔵の実物写真）
- `xiwei-feidi-yuanqin`（西魏・廃帝 元欽）と同名の `北魏《元钦墓志铭》`＝**CC BY-SA 4.0**（2023年撮影）。
  題は「北魏」で**本人とは限らない**（同じプールに宣武帝の妃の `元恪嫔李氏墓志` も混じる）
- `liao-daozong`（遼・道宗）の契丹小字哀冊＝実物写真が **CC BY 4.0**、**拓本のスキャンだけが PD**

**最初の突合は7件しか当てられず、新字体→繁体/簡体の変換を掛けて引き直したら67件になった**
（当たりが増えたぶんは陵墓の現代写真・出土文物・別人・肖像で、皇帝本人の墓誌は上の1件だけ増えた）。
この文書の11行が「その変換で取りこぼしが18件→0件になった」と書いてあるのに踏んだ罠なので、
**次にファイル名と人名を突き合わせるときは必ず `scripts/hanzi_norm.py` の `norm_for_match` を
両側に掛ける**（`R-SWEEP-DETECTION`・引き直しは外部リクエスト0で済む）。

**Issue #81 が予測した「拓本のスキャンは PD-scan で通る／碑を現地で撮った写真は CC BY-SA 帯」は
実測どおりだったが、通る側の母数が1件しかなかった。** 3次元物体の撮影に撮影者の権利が立つのは
A-4（陵墓の現代写真・50件中 CC BY-SA 44件）を落としたのと同じ理屈で、**墓誌・碑の実物写真にも
そのまま掛かる**（＝A-2 のうち実物写真クラスは A-4 と一緒に落ちていた）。

残った1件も**面としては使えない**。実物は契丹小字がびっしり並ぶ黒地の拓本で、カードの肖像枠
（3:4のほぼ正方形）にどこを切っても「黒い文字の面」にしかならず、図版キャプションまで写り込んでいる。
**1人のために `manifest.json` のスキーマを分ける（`focusY` は非肖像で意味を失う）価値はない。**

`Epitaphs in China` 171件・`Epitaphs of the Northern Wei Dynasty` 3件などの中身は、ほぼ全部が
**臣下・后妃・僧の墓誌**だった（皇帝の妃 `元恪嫔李氏墓志` のように皇帝名を含むが本人ではないもの
が混じるので、人名の部分一致だけで採らないこと）。**皇帝は帝としての葬礼を受けるので墓誌ではなく
哀冊・陵碑になり、その哀冊・陵碑は現地の石なので現代写真＝CC BY-SA 帯へ落ちる** — この構造が
「拓本は PD なのに皇帝の拓本が無い」の中身で、探し方を変えても埋まらない。

### 副産物: 肖像候補が1件出た（`tang-jingzong`・**採用した**）

引き直しの中で **`帝鉴图说…(唐敬宗) (cropped).jpg`（CC0・彩絵帝鑑図説／フランス国立図書館蔵・18世紀）**
が出た。単独の騎馬人物を切り出したもので、カード枠には人物だけが入る。同じ故事の版本
`便殿擊毬唐敬宗.jpg`（PD・帝鑑圖說 純忠堂藏板）もあるが、こちらは人物が小さく複数で
2026-07-31 に `wei-caofang` を落とした基準（版本の場面図）に該当する。

**2026-08-02 の全数探索がこれを拾えなかった経路は Wikidata P373 の英語カテゴリ**
（`Emperor Jingzong of Tang`）で、当時は `intitle` の CJK 検索と ja/zh のリード画像しか
持っていなかった（`portrait-candidates-2026-08-02.json` の `coverageNote` が
「Latin 表記のファイル名には intitle が原理的に当たらない」と予告していた穴そのもの）。
**残り182人ぶんの P373 を持たない皇帝には、この経路でも入口が無い**（残量の行は
[RESIDUAL.md](../process/RESIDUAL.md)）。

**ユーザーの判断で採用した**（2026-08-05・確定数は 154→155人）。反映は下の
「副産物による追加（2026-08-05）」節。

### A-1（年号銭）の再開条件（2026-08-05 時点で保留）

**ライセンス面は A-2 と違って通りうる** — Commons には銭の拓本に `PD-Coin rubbing` テンプレートが
確立していて421件、`Rubbings of coins of China` が41件ある（銭そのものの写真は3次元物体なので
CC BY-SA 帯だが、拓本は2次元の忠実複製）。**塞がっているのは突合側**で、元号名が
`data/emperors.json` の `eraChangeCount.events[].eraName` に **681件中5件**しか無く、候補を機械生成
できない。**再開の条件は Issue #37（元号の悉皆補充）が元号名を埋めること。** note の散文から
元号名を読み取って代用しないこと（`R-COVERAGE-MEASURED`）。

## 副産物による追加（2026-08-05）— `tang-jingzong`（唐敬宗）

**確定数は154→155件。** 上の A-2 調査（拓本・碑刻）の再突合で出た候補を、ユーザーの判断で採用したもの。

| 項目 | 内容 |
|---|---|
| id | `tang-jingzong`（唐・敬宗／李湛） |
| Commons | [File:帝鉴图说….95帧图.彩绘册页.约18世纪 (唐敬宗) (cropped).jpg](https://commons.wikimedia.org/wiki/File:%E5%B8%9D%E9%89%B4%E5%9B%BE%E8%AF%B4.Recueil_Historique_des_Principaux_Traits_de_la_Vie_des_Empereurs_Chinois.95%E5%B8%A7%E5%9B%BE.%E5%BD%A9%E7%BB%98%E5%86%8C%E9%A1%B5.%E7%BA%A618%E4%B8%96%E7%BA%AA_%28%E5%94%90%E6%95%AC%E5%AE%97%29_%28cropped%29.jpg)（原寸 629×631） |
| 出典 | 『Recueil Historique des Principaux Traits de la Vie des Empereurs Chinois』（彩絵帝鑑図説・18世紀・フランス国立図書館蔵）。投稿は書格（User:Zeroiszen） |
| ライセンス | **CC0**（`AttributionRequired: false`） |
| `focusY` | **0.28** |

**同じ id を 2026-07-21 の目視QAで「場面図切り出し」として落としているが、あれは別ファイル**（版本
『帝鑑圖說』の `便殿擊毬唐敬宗.jpg`＝人物が小さく複数）。今回のものは同じ撃鞠の故事を彩絵本から
**単独の騎馬人物として切り出した**もので、`tangmo-anlushan`（2026-08-02 に採用・同じ彩絵帝鑑図説の
cropped・CC0）と同じクラスにあたる。上端に隣の騎手の馬の脚が残るが、カード枠（3:4のほぼ正方形）に
実際に切って確認したところ人物が枠の中心に入る。`portraits-candidates.json` の
`excludedQa2026_07_21.sceneCrop` の記録は消さず、`note2026_08_05` で「別ファイルで再採用した」と
断ってある — **除外の記録を上書きすると、同じ絵をもう一度採ろうとしたときに歯止めが消える。**

この1件もダンプ照合（`page_image_free`）の産物ではないため、`manifest.json` の `matchedWikiTitle` と
`portraits-candidates.json` の `matchedTitle` は **`null`**（`ming-yizong` と同じ扱い）。取得は既存と同じく
Commons のファイル名MD5からURLを算出して幅500pxサムネイルを1リクエスト、ライセンスは Action API の
`extmetadata` を1リクエスト。

## 画像ダウンロード（2026-07-17）

機械照合の168名分の画像本体を取得し、`data/images/portraits/` に配置（目視確認で13名分、CC BY-SA方針除外で2名分を除外、この時点で**153ファイル**。2026-07-21の目視QAでさらに3件除外して150ファイル、2026-07-31にさらに6件除外して144ファイル、2026-08-01に1件追加して145ファイル、2026-08-02に9件追加して154ファイル、2026-08-05に1件追加し現在は155ファイル）。

- **保存形式**: `{id}.jpg` / `{id}.png`（`data/emperors.json`の`id`フィールドと1対1対応、サイト側でのファイル名からの逆引きが容易）
- **取得方法**: オリジナル画像ではなく**幅500pxのサムネイル**を取得（[Wikitech Robot policy](https://wikitech.wikimedia.org/wiki/Robot_policy)・[API:Etiquette](https://www.mediawiki.org/wiki/API:Etiquette)に基づき、原寸よりサムネイルを優先・リクエスト間隔1秒以上・同時接続数1を遵守）。オリジナルのまま取得すると数十MB級のファイルが混在することが判明したため（例: 宋徽宗43MB）、サムネイル方式に変更した
- **合計サイズ**: 約19MB（153ファイル、平均約130KB）
- **URL特定方法**: Wikimedia Commonsのアップロード先URLはファイル名のMD5ハッシュから機械的に算出できる（`upload.wikimedia.org/wikipedia/commons/thumb/{md5[0]}/{md5[0:2]}/{filename}/{width}px-{filename}`）ため、API（`api.php`/`rest_v1`、レート制限で問題が起きた経路）を一切経由せず、静的配信CDNへの直接アクセスのみで完結した
- **出典マニフェスト**: [`data/images/portraits/manifest.json`](../../data/images/portraits/manifest.json) に、各ファイルの元Commonsファイル名・CommonsページURL・ライセンス情報を記録（非肖像画と判定された13件・CC BY-SA方針除外の2件は削除済み）

## ライセンス確認（2026-07-17）

ダウンロード直後の168件全件について、Commons Action API（`prop=imageinfo&iiprop=extmetadata`、1リクエストで最大50件までタイトルをまとめて問い合わせ可能なため、168件でも約4リクエストのみで完了。以前レート制限で問題を起こした逐次アクセスとは異なる使い方）でライセンス種別・作者・クレジット情報を取得。その後、目視確認で非肖像画と判定した13件、およびCC BY-SA方針により2件を除外し、この時点の153件は全件`licenseVerified: true`かつPublic domain/CC0のみとなった（2026-07-21の目視QAでさらに3件除外、2026-07-31のカード表示での見直しでさらに6件除外して144件、2026-08-01に Public domain の1件を追加して145件、2026-08-02に Public domain 8件・CC0 1件を追加して154件、2026-08-05に CC0 1件を追加し**現在155件**。全件`licenseVerified: true`である点は変わらない）。

| ライセンス | 件数（2026-07-17時点・153件中） | 件数（現在・155件中） |
|---|---|---|
| Public domain | 150 | 152 |
| CC0 | 3 | 3 |

- **要クレジット表記が必要な画像は0件**。CC BY-SA 4.0だった2件（`beiwei-daowudi`、`shiguo-qianshu-wangjian`）は「CC BY-SA系は使用しない」という方針決定により除外したため（掲載時のクレジット表記対応が不要になった）
- Public domain / CC0（全件）は法的な表記義務はないが、出典明示は望ましい
- 目視確認で除外前に要クレジット表記だった10件（`beiwei-houfeidi-yuanlang`等）は、非肖像画だったため既に除外済み — **CC BY-SA系（現代のCommons投稿者が撮影した写真）は、PD/CC0系（歴史的絵画のデジタル化）と比べて肖像画ではない誤マッチ率が明らかに高かった**。今後同様の照合を行う際は、CC BY-SA系ヒットは特に目視確認を優先するとよい

## 未確認・今後の注意点

- **`page_image_free`はあくまで「自由ライセンスの画像がある」ことを示すフラグであり、画像が肖像画かどうか・具体的なライセンス種別（PD/CC-BY/CC-BY-SA等）は含まれない**。今回13件で実際に非肖像画（風景・石窟等）が混入したことからも、**機械照合だけで完結させず必ず目視確認を挟む**必要がある
- 219名の「画像なし」は、zh.wikipediaの`page_image_free`が未設定・CC BY-SA方針除外・目視QAでの除外なだけで、Commons側や他ソースには画像が存在する可能性がある（Commonsダンプでの追加調査は、圧縮時点で数十GB規模になり今回の目的に対して不釣り合いと判断し見送った）。実装時に台北故宮オープンデータや個別のCommons検索で追い足す余地あり（その際もCC BY-SA系は対象外とする）
- 確定した画像も、故宮系肖像画・晩笑堂竹莊畫傳系の複製画像など出典が混在しているため、掲載前に出典・ライセンス種別を人物ごとに改めて確認する（2026-07-21の目視QAで実際に3件の誤り・不適合が見つかった）
