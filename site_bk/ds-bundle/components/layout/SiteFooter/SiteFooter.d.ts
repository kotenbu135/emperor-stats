import * as React from 'react';

/**
 * SiteFooter — from site@0.1.0.
 */
export interface SiteFooterProps {
  [key: string]: unknown;
}

export declare const SiteFooter: React.ComponentType<SiteFooterProps>;
