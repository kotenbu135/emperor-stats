# 見た目強化＋SEO 改修 TODO（2026-07-27・未着手）

`AUDIT_2026-07-27-visual.md` の全指摘に、SEO の検証結果を加えて着手順に並べ直したもの。
**この時点で実装は一切していない。** 監査時の判断で保留にしていた3点は、ユーザー決定により下記のとおり確定した。

| 監査での保留 | ユーザー決定（2026-07-27） |
|---|---|
| A-6 円グラフのラベル方式 | **外側ラベルを廃止**。両方とも弧内ラベルに揃える |
| A-5 即位時年齢ランキング | **年長順に変更** |
| B-1・B-2 の契約変更 | **DESIGN.md の改訂を許可** |

**着手前のベースラインは撮影済み**（`tools/before-desktop/`・`tools/before-mobile/`・2026-07-27）。
`/kinship` はバイト一致、`/timeline` は行ハッシュまたはグリフ bbox で同一性を確認すること。

---

## 1. DESIGN.md の改訂文を先に起草・確定する（コミットは各実装と同時）

**文面を先に決める**。実装してから DESIGN.md を追いかけて書くと「実装が正・文書が後追い」になって次の監査で拾えなくなる。
ただし**コミットは対応する実装と同じコミットに入れる**（1-1 は 6 と、1-2 は 10 と、1-3 は 11 と）。
契約だけ先にコミットすると、実装までのあいだ文書と実装が食い違う。1-4・1-5 は対応する実装（7・8）と同時。

- **1-1. 円グラフの節を「弧内ラベルのみ」に書き換える**
  現行は「列幅と最長ラベルで外側ラベルの可否を決める」という規定。外側ラベル廃止に伴い、
  「割合は弧の中に置き、入るならカテゴリ名も併記し、入らないものは凡例と表で識別させる」だけを残す。
  「ラベルを省略・短縮しない」の禁止事項は維持。
- **1-2. Layout 節のスクロール規定を縦にも広げる**
  現行「横スクロールする領域には手掛かりを必ず伴わせる」に、縦スクロール枠の端フェードを追加する（→ 9）。
- **1-3. Components 節のモノグラム下地の濃度指定を見直す**
  現行「最も淡い濃度」。実際に並べると読み込み中の枠に見えるため、濃度を上げる方針に改める（→ 10）。
- **1-4. 凡例の位置に規範を1行足す**（→ 6）
- **1-5. `ChartTakeaway` の置き場所に規範を1行足す**（→ 7）
- **完了条件**: `npx @google/design.md lint DESIGN.md` が errors 0 / warnings 0。

**据え置きを明文化するもの（再提案しない）**
- **時代バンド（トップ「時代ごとの皇帝数」）を無彩色のまま残す**。時代区分は王朝ではないため王朝色を当てられない。
  色を与えると「時代区分の色」という新しい意味の軸を作ることになる（監査 B-3・現状維持で決定）。
- **ダークモードは実装しない**（既存の Do's and Don'ts のまま）。

---

## 2. `/emperors/[id]` に面（`--card`）を与える  ※監査 A-1

基本情報 `dl`・回数グリッド・在位中の出来事リストの3ブロックを `--card` の面に載せる。新しいトークンも影も不要。

- 対象: `src/components/emperors/emperor-detail-body.tsx`・`emperor-event-timeline.tsx`
- **`EmperorDetailBody` は詳細ダイアログと共用**（`emperor-detail-dialog.tsx`）。ダイアログはそれ自体が面なので、
  中でカードを重ねると面が二重になる。**`wide` prop（＝個別ページ）でだけ面を出す**か、面の有無を prop で受ける
- このルートは `contained containedWidth="max-w-4xl"`（`app/emperors/[id]/page.tsx:107,113`）。
  カードは中央寄せ列の**幅いっぱい**に置く。カード側に外側マージンを足すと見出しの朱バー左端と本文左端がずれる
- 完了条件: 1440px・375px で `/emperors/[id]` を撮り、ダイアログ（`/emperors` から開く）の描画が変わっていないこと

## 3. `/emperors/[id]` の情報の並びを直す  ※監査 A-2

- 脚注「順位は同数を同順位として数えています…」（`emperor-detail-body.tsx:231`）を**回数グリッドの直下**へ移す
- `諡号：…`（`:233`）は基本情報 `dl` の行にする
- 関連動画セクション（`:207`〜）は経緯の散文より下（本文末尾側）へ移す
- 完了条件: 説明文と説明対象のあいだに無関係なブロックが挟まらないこと。ダイアログ側の `collapseVideos` 分岐は壊さない

## 4. `/emperors/[id]` のモバイルを縦積みにする  ※監査 A-3

375px で肖像（約140px）と基本情報 `dl` が横並びのため `dl` の実効幅が約200pxしかなく、
「172名中・若い順53位タイ」が3行に折り返す。

- 対象: `emperor-detail-body.tsx:102` 付近の flex
- `sm` 未満は肖像を上・`dl` を下へ積む。肖像の枠比 3:4 と `object-position: top` は変えない
- 完了条件: 390×844 で値の折り返しが解消し、行の高さが揃うこと

## 5. フィルタのセレクトを狭い画面で全幅にする  ※監査 A-4

375px で右に約180pxの空白が残り、ラベル・セレクト・件数表示が左に寄った不揃いな段になる。

- 対象: `charts/chart-filter-controls.tsx:113`（`w-[170px]`）・`:132`（`w-[180px]`）、
  `charts/group-filter-controls.tsx:50,62,79`（`w-[130px]`/`w-[170px]`/`w-[180px]`）、
  `charts/dynasty-combobox.tsx:47,78`（`w-[200px]`）
- `w-full sm:w-[…]` の形にする。固定幅の理由（フォント読み込みによる折り返しずれ＝CLS）は、
  375px ではフィルタ行がすでに1列に積まれていて折り返す余地が無いため抵触しない
- 完了条件: 375px で CLS を実測して 0 のままであること。デスクトップの描画は不変

## 6. 円グラフの外側ラベルを廃止する  ※監査 A-6（ユーザー決定）

`/death-accession` の左右のドーナツで、ラベル方式も描画半径も揃っていない状態を解消する。

- 対象: `charts/category-pie-chart.tsx`
  - `useOutsideLabels` を廃止し、`arcLinkLabels` 系（`:244`〜`:250`）と layers の分岐（`:260`）を弧内ラベルに一本化
  - **幅計測の `ResizeObserver`（`:56`〜）が他の用途に使われていないことを確認してから外す**。
    外側ラベルの可否判定にしか使っていないなら、周辺の定数（`:39` の消費幅コメント）ごと削除する
- **実数（161・96・35…）の行き先を先に決める**。現在の死因ドーナツは弧の**内側に実数**・**外側に「病死 44%」**を出しており、
  即位経路ドーナツ（＝目標の形）は弧の内側が「世襲 33%」だけ。単純に揃えると**図から実数が消える**。
  弧の中を「カテゴリ名＋%」にするか「%＋実数」にするかを決め、消えた側が凡例か「表で見る」から辿れることを確認する
  （DESIGN.md の円グラフ節が許すのは「入らないものは直下の凡例と表で識別させる」まで。現在の凡例はスウォッチ＋名前だけで実数は無い）
- **外側ラベル用に空けていた margin が浮くのでドーナツが太る**。中央の総計（`CenteredTotal`）や凡例と接触しないか確認する
- 描画半径が左右で揃うこと、どの幅でもラベルの切れが出ないことを 1440 / 1024 / 768 / 375px で確認
- **代償を承知の上での決定**: 広い画面では外側ラベルを出せるのに出さなくなる（計画06 の列幅適応を捨てる）
- 完了条件: DESIGN.md 1-1 の書き換えと同じコミットに入れる

## 7. 凡例の位置を統一する  ※監査 A-7

- 現状: 円（`category-pie-chart.tsx`）はグラフの**下**、積み上げ棒（`dynasty-death-cause-chart.tsx`）は**上**
- **下に揃える**ことを推奨（円グラフ2面 + 表ビューの並びと自然につながる）。DESIGN.md 1-4 に1行足す
- ランキング棒に凡例を付けないのは正しい（各行にラベルがあるため）。変更しない

## 8. `ChartTakeaway` の置き場所を統一する  ※監査 A-8

- 現状: `/death-accession` はページ先頭に1つ、`/reign`・`/military`・`/dynasties`・`/ages`・`/court-events` は
  先頭セクションの中に1つ。実質同じ規則が2通りの書き方になっている

**「ページ先頭に1つ」への統一は採らない**（`getChartTakeaway()` の実文を読んで判明）。
`/military` の総括文は**親征だけ**の話（`emperors.ts:1217`・3節あるうちの1節）、`/court-events` は**改元だけ**（`:1209`・5節）、
`/dynasties` は**平均在位年数だけ**（`:1226`・2節）、`/ages` は**即位時年齢だけ**（`:1201`・2節）。
h1 直下へ持ち上げると、節に属する主張がページ全体の主張として読める。

- **規範は「総括文は、その内容が対象とする節の中に置く」**とする（DESIGN.md 1-5）
- したがって**動かすのは `/death-accession` の1ページだけ**。ここの総括文は2文で、
  1文目が死因・2文目が即位経路と**すでに節ごとに分かれている**（`emperors.ts:1194`〜）。
  配列を節に振り分けるだけで済み、**新しいコピーは1文字も書かない**
- 対象: `app/death-accession/page.tsx`、`lib/emperors.ts` の `getChartTakeaway("death-accession")` の呼び分け
- **破棄済み計画10（統計3ページへのリード文追加）とは別物**。新しい散文を1文字も作らない
- 完了条件: 全統計ページで、総括文がその内容の対象節の中にあること。他5ページは無変更

## 9. 即位時年齢ランキングを年長順にする  ※監査 A-5（ユーザー決定）

既定が若い順のため、初期表示でカード幅の85%以上が空の格子になる（軸は86歳まで、上位20行は1〜8歳）。

**採る解釈**: 表示の既定並び順だけでなく**順位の定義も年長順へ統一する**（rank 1 = 最年長）。
`RANK_DIRECTIONS` で即位時年齢だけが `asc` の例外になっており（`emperors.ts:376`）、これを揃えると例外が消える。
表示だけ年長順にして rank 1 = 最年少を残すと、先頭行に「172位」と出る。
（もし表示順だけを変える意図だったなら、この項目だけ差し戻すこと。）

- 対象:
  - `lib/emperors.ts:376` の `RANK_DIRECTIONS.accessionAge`（`asc` → `desc`）と `:1202` のコメント
  - `app/ages/page.tsx` の並び順の既定値と、節の説明文「幼くして即位した皇帝から順に表示します」
  - `getChartTakeaway("ages")`（`emperors.ts:1201`〜）— 「最も若くして即位したのは…」が「最も年長で…」になる。
    **総括文はデータから導出される文なので、ここの書き換えは新規コピーではない**
  - `site/AGENTS.md` の「即位時年齢だけ若い順」という記述
  - ※ `/about` には順位の向きの記述は無い（grep 済み）。触らなくてよい
- **`/emperors/[id]` と詳細ダイアログの順位表記が365人分すべて動く**（例:「172名中・若い順53位タイ」→「年長順◯位タイ」）。
  個別ページのスクリーンショット差分は全件で出る前提で確認する
- 「軸を表示中の範囲に追従させる」案は**採らない**（検証済み）。`maxValue` はフィルタ後の全件から取り、
  Nivo に渡す行だけが `useWindowedRows` で切られているため、追従させるとスクロールのたびに軸が伸縮する

## 10. 縦スクロール枠に端フェードを与える  ※監査 B-2（DESIGN.md 改訂を伴う）

ランキング棒グラフは枠の下端で必ず行が途中で切れる。横スクロールには端フェード＋バッジがあるのに縦には何も無く、
切れた行が実装漏れに見える。

- 対象: `charts/horizontal-scroll-hint.tsx` に縦版を足す（部品を1つにまとめるか分けるかは実装時判断）、
  `charts/scroll-bar-chart.tsx` の枠に適用
- **背の低い枠では告知バッジを出さない**という既存の判断を縦にも引き継ぐ（フェードのみ）
- **既存の性能対策を退行させない**: 行ウィンドウイング・`transform: translateY` での移動・
  スクロール直後150msのホバー抑制。フェードは重ね描画なのでスクロール中の再レンダリングを起こさないこと
- 完了条件: `/emperors` グリッド上と各ランキングで Long Task 0件・CLS 0 を実測。DESIGN.md 1-2 と同時にコミット

## 11. 一覧カードのモノグラム濃度を上げる  ※監査 B-1（DESIGN.md 改訂を伴う）

実際の肖像（濃い色面・線描・淡彩がまざる）と、王朝色22%の極淡地に薄い1文字のモノグラムが同じ枠に並び、
モノグラム側が読み込み中の枠に見える。

- 対象: `components/emperors/portrait.tsx:44`（`dynastyColorMix` の濃度）と文字色
- 下地の濃度を上げるか、文字の濃度を上げるかは実装時に両方試して比較する
- **塗りの上の文字色は混色後の実値からコントラスト比で選ぶ**（既存の `readableTextOn` の原則）
- 完了条件: `/emperors` を1440px・375pxで撮り、肖像ありのカードと並べて明度差が縮んでいること。DESIGN.md 1-3 と同時に

---

## SEO（`out/` の実出力を検証した結果）

既存の実装は概ね堅い。**Dataset JSON-LD の配布ファイル（`/data/emperors.json`・`.csv`）は実在**、
個別ページの `sameAs` は目視確認済み Wikidata QID 由来、`/kinship` は `noindex` かつ sitemap 未登録、
404 は `noindex`、肖像の `alt` あり、パンくずは個別ページで3階層まで正しく出ている。以下は実際に見つかった穴。

## 12. 統計6ページの meta description を書き足す  ※SEO

実出力の文字数（`out/*.html`）:

| ページ | 文字数 | 内容 |
|---|---|---|
| `/death-accession` | **12** | 死因別・即位経路別の内訳 |
| `/military` | **19** | 親征・反乱鎮圧・被反乱の回数ランキング |
| `/ages` | **20** | 即位時年齢・没年齢のランキング（数え年） |
| `/dynasties` | **22** | 平均在位年数・死因の内訳を王朝単位で横断比較 |
| `/emperors` | **22** | 全皇帝の図鑑。名前・王朝で検索し、詳細を表示 |
| `/reign` | **23** | 在位年数ランキングと復位者（複数回即位）の一覧 |
| `/court-events` | **25** | 改元・大赦・立后・皇太子廃立・遷都の回数ランキング |
| （参考）`/timeline` | 63 | 手書きの散文 |
| （参考）`/emperors/[id]` | 71 | 自動生成 |

`SITE_SECTIONS.description` は**トップのカード本文**として設計された短い見出しで、
`sectionDescription()` 経由でそのまま meta description に流用されている。検索結果のスニペットには情報量が足りない。

- **既存の先例に倣う**: `/timeline` は `SITE_SECTIONS` の短い文（26文字）を使わず、
  `app/timeline/page.tsx:14` で `buildMetadata({ description })` に手書きの63文字を直接渡している。
  同じやり方で6ページにも書き足す。**`SITE_SECTIONS` にフィールドを足す必要はない**
- 対象: `app/reign|death-accession|court-events|military|ages|dynasties/page.tsx` の `metadata`
- `/timeline` と同程度（60〜90文字）を目安に、各ページの具体的な数値・固有名を含める
- **`SITE_SECTIONS` は触らない** — トップのカード本文としての1対1対応と drift 防止はそのまま維持する

## 13. 統計6ページの `<title>` を具体化する  ※SEO

現状は内部のナビゲーションラベルがそのまま `<title>` になっている:
`年齢 | 中国皇帝統計`・`軍事 | 中国皇帝統計`・`在位データ | 中国皇帝統計` など2〜7文字。
検索クエリとの一致面積が小さい。

- 対象: 各 `app/*/page.tsx` の `buildMetadata({ title })`
- サイドバーの表示ラベルとは**別の文字列**にする（ナビは短いままが正しい）。
  例: `年齢` → `即位時年齢・没年齢ランキング`、`軍事` → `親征・反乱鎮圧・被反乱の回数ランキング`
- `title.template`（`%s | 中国皇帝統計`）はそのまま。全角で30〜35文字を超えないこと

## 14. `/emperors` に一覧の構造化データを足す  ※SEO

365件の一覧ページに `BreadcrumbList` しか出ていない（`out/emperors.html` を確認）。

- `CollectionPage` + `ItemList`（`ListItem` に名前と URL）を足す
- **365件すべてを列挙するとHTMLが太る**。上位N件に絞るか `numberOfItems` だけ持たせるかは実装時に判断し、
  出力サイズの増分を実測して記録する
- 値はこのサイト自身が生成したデータのみを渡す（`JsonLd` の既存の原則）

## 15. `/kinship-source` をクローラから外す  ※SEO

`app/kinship-source/route.ts` が `out/kinship-source` として配信されている。HTMLではないため
`noindex` メタを付けられず、`/kinship` 本体が `noindex` のあいだも系譜データだけクロール可能な状態。

- 対象: `app/robots.ts` に `disallow: ["/kinship-source"]` を足す
- `/kinship` を全章そろえて公開する際に、この Disallow も一緒に外す（→ メニュー追加・noindex 解除と同じタイミング）

## 16. 統計ページから Dataset への関連付け  ※SEO・優先度低

`/about` にだけ `Dataset` JSON-LD があり、統計ページからの関連付けが無い。
各統計ページに `WebPage` + `isPartOf`（`/about` の Dataset を指す）を足すと、
どのページも同一データセットの可視化であることが機械可読になる。

効果は限定的なので、12〜15 が済んでから着手する。

## 17. 設計記録へ追記する（最後に必ず）

`site/AGENTS.md` は「大きめの UI 変更をしたら該当する設計記録へ追記する」ことを求めている。本 TODO は11件の UI 変更を含む。

- 追記先: `docs/site-design/REDESIGN_2026-07.md`（2026-07-27 の再構成の続きとして）
- 書くこと: DESIGN.md の改訂差分の要約（1-1〜1-5）、外側ラベル廃止で捨てた適応の記録（計画06 の顛末）、
  即位時年齢の順位定義変更、`/kinship` のバイト一致・`/timeline` の行ハッシュ／グリフ bbox 一致の検証結果
- `design-plans/README.md` の「計画11件の顛末」表と同じ場所に、本 TODO の顛末も1行で残す

---

## 着手していないもの（記録として残す）

- **サイドバー幅の変更** — 幅を変えると `/timeline` が再フィットし、`README.md` の絶対条件2
  （`/timeline` の描画を1ピクセルも変えない）に抵触する。**凍結を解除する明示的な判断が出るまで着手しない**
- **ダークモード** — DESIGN.md で禁止（→ 1 の据え置き明文化）
